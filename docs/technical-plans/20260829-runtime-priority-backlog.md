# 运行时待办优先级排期（2026-08-29）

依据：run `07158ad8` 诊断结论 + `20260829-compaction-production-fix.md` §6-§9 决议。
排序公式：收益 × 低风险优先 + 依赖前置（F1/F2 的 Langfuse 验收依赖观测链路）。

## P0 — 立即（低风险、高性价比、或解锁后续验收）

| # | 事项 | 收益 | 风险 | 成本 | 依据 |
|---|---|---|---|---|---|
| P0-1 | **Langfuse 生产写入链路修复**（核查已做，直接修） | 恢复 LLM 成本/缓存可观测性（生产全盲）；**F1/F2 部署验收（Q5-a）依赖 Langfuse 现场指标** | 低-中（根因候选已锁定，diff 两个 commit 定位） | 低-中 | ✅ 已实锤：生产开了观测（LF_HOST=host.docker.internal:3000=本机 langfuse 容器，端口映射已验）；24h 内 03:43 北京后开始的 17 个 run 全部零 trace，03:43 前的有；**根因候选=aff7bf70（run 终态第一方评分）或 9d6fd580（根 span 补 output 摘要），均 04:03-04:15 上线**，与断流窗口吻合 |
| P0-2 | **token_tracker watchdog 误报抑制**（`token_tracker.py:238`） | 消除结构性误报噪声（该 agent 已 3 次 95-97% miss 假警报），保住告警信噪比；F1/F2 上线后压缩更频繁，不修则误报更多 | 极低（只改告警触发条件，不动计量/计费） | 小（~1h 含测试） | ✅ 三次警告全部对齐压缩后第一步（step 32/50/59），前缀重建必然 miss。⚠️ 改法修正：`record_token_usage` 调用点在 caller.py（8 处）+single_step.py:205，**LLM 调用层没有压缩上下文**（anchor 指纹在 model_step_service 层），「anchor 首步豁免」需跨层传参、成本被低估；推荐改为 **冷却窗口**（同 agent 每 N 分钟最多一次）或**连续 ≥2 步才报警**（agent 表加计数），或把告警上移到压缩发生点附近 |
| P0-3 | **F3：cjk_aware 估算 + 三处接入**（commit F3，票 04） | 修正中文 1.7× 高估（再实锤：provider input=6044 vs 指纹估算 10226）；预算/压缩触发/截断计量基准全部转正，是后续所有验证的计量前提 | 低（纯估算函数，不发给 provider；压缩触发更早属预期，§7 已列） | 小-中 | ✅ 三处调用点核实无误：caller.py:205、run_compactor.py:156、model_step_service.py:677，当前均为 `chars_per_token=4, utf8_bytes=True`；澄清：`cjk_aware` 是**新增参数**（现函数无此选项），非现成开关 |

## P1 — 核心修复（按依赖顺序）

| # | 事项 | 收益 | 风险 | 成本 | 依据 |
|---|---|---|---|---|---|
| P1-1 | **F1：8 节结构化指令 + shrink 校验**（commit F1，票 02） | 高——直击压缩 74% 重述（最大 token 浪费源） | 中（prompt 行为变化；§7 缓解：shrink 不设下限、degraded fail-open） | 中 | 方案 §3；F-C 决议（防重述载体=模板，shrink=安全网） |
| P1-2 | **F1.5：`_summary_from_step` 结构校验**（可选小 commit） | 低-中（温和结构校验，防格式损坏） | 极低 | 小 | Q6 决议；可单独回滚 |
| P1-3 | **F2：压缩请求复用主请求 cache-stable 前缀**（commit F2，票 03） | 高——压缩请求自身 5-6K 全价 miss→命中；**并直接缓解本次诊断的「压缩后第一步 96% miss」**（摘要段已缓存） | 中-高（两处构造漂移；invariant 门禁缓解，§4.4/F-B） | 高 | 方案 §4；F-A/F-B/Q2-b 决议；依赖 F1 模板常量 |

## P2 — 体验与跟进

| # | 事项 | 收益 | 风险 | 成本 | 依据 |
|---|---|---|---|---|---|
| P2-1 | **前端「执行中无进度」判据核实**（先只读定位） | 用户主诉的可见体验：`agent_runs.updated_at` 停在 17:15:35 而 run 活跃到 17:37+，idle 25min 是表象；若前端用 updated_at/轮询判活跃则需修复 | 低-中（先定位判据再动；可能涉及轮询/事件游标改造） | 中 | 前端未 grep 到「执行中」文案与 delivery_status 渲染，判据位置待定位；真活跃信号在 `agent_tool_executions.updated_at`/事件流 |
| P2-2 | **新票同步 .scratch**：P0-1/P0-2/P2-1 各立一票 | 追踪闭环 | 无 | 小 | 与 compaction-slimming 票 02/03/04（已改写）同目录；01-tool-result-pruning 已降级（§8） |

## 执行顺序（依赖视角）

```
P0-1 Langfuse 核查 ──(并行)──→ P0-2 watchdog ──(并行)──→ P0-3 F3
        │（为 F1/F2 验收铺路）
        └──────────────────────────────→ P1-1 F1 → P1-2 F1.5 → P1-3 F2
                                                        P2-1 前端判据（可随时并行）
```

部署节奏沿用方案 §6.1：每 commit 独立可回滚；F1/F2 部署后用 Langfuse 现场指标对照事故样本 a4b1a018 验收（141s→≤单次、74%→≤50%、cache_read>0）。

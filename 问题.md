# Clawith 后端 + IDEA 插件 全量代码评审 — 问题清单

> 评审时间：2026-05-12 | 更新：2026-05-14（第 10 轮，新增会话流畅度瓶颈分析——12 项跨层延迟源 + 4 项 UI 优化）
> 评审范围：Clawith 后端 `backend/` (365 个 Python 文件) + IDEA 插件 `demo-new/` (1361 个 Kotlin/Java 文件)
> Qodana 检查：5265 个问题（Critical 41 / High 3850 / Moderate 1374）
> 详细分析文档：`会话逻辑分析报告.md`（架构概览、数据模型关系、最佳实践对照表）

**共 42 个待处理问题**：🔴7　🟠2　🟡20　🟢13

---

## 🔴 严重（7 个）

### #1 — 智能体无法感知本地项目上下文

**原编号**: L3 | **位置**: `jsonrpc_router.py:129-141` + `_build_ide_environment_prompt()`
**状态**: 🔄 后端上下文注入已完整，根因已定位（见 #55、#56、#57），需端到端验证

**三层断裂**：
1. 上下文注入链路 — `_build_ide_environment_prompt()` 已解析 `chatContext`（activeFilePath, selectedText 等）+ `sessionType` + `mode` + 代码地图注入，但上下文仅注入 LLM 提示不持久化（#57）
2. 工具可用性感知 — 系统提示词未说明"已连接本地IDE"，无首次连接环境注入消息
3. 工具调用反馈闭环 — UI 层看不到过程，LLM 层可能不知道如何利用返回结果

**根因链**（新增 2026-05-14 会话逻辑审查）：
- #55：`ChatSession` 模型缺少 `client_type`, `project_path`, `current_file`, `open_files` 四个 DB 列映射
- #56：`SessionContextManager`（`session_context.py`）三个方法全局零调用方
- #57：`_persist_lsp4j_conversation()` 创建会话时未设置 IDE 上下文字段

<details>
<summary>LSP4J 未适配功能 & 修复规划</summary>

**未适配功能**：

| 功能 | 协议方法 | 优先级 |
|------|---------|--------|
| 内联编辑操作 | `textDocument/nextEditAction` | P1 |
| 代码变更应用完成 | `chat/codeChange/apply/finish` | P1 |
| 快照同步 | `snapshot/syncAll` | P2 |
| 工作空间文件同步 | `workingSpaceFile/sync` | P2 |
| 聊天删除通知 | `chat/delete` | P2 |
| 全局配置变更 | `config/changeGlobal` | P2 |
| 模型列表刷新 | `config/refreshModels` | P2 |
| 代码引用查询 | `textDocument/queryReference` | P3 |
| PSI继承分析 | `psi/inherits` | P3 |
| PSI变量分析 | `psi/listVariables` | P3 |

**修复阶段**：

| 阶段 | 内容 | 状态 |
|------|------|------|
| P0 | 全局 Map 初始化、tool/call/sync 通知、requestId 传递 | ✅ 已完成 |
| P1 | 增强上下文提取、增强工具可用性系统提示 | ⬜ 待推进 |
| 体验优化 | Web 端工具调用可视化、Diff 预览、高风险工具确认 | ⬜ 待规划 |

</details>

---

### #55 — ChatSession 模型缺少 IDE 上下文字段（运行时 `AttributeError`）

**来源**: 会话逻辑专项审查 (2026-05-14) | **位置**: `backend/app/models/chat_session.py`
**状态**: ⬜ 待修复

数据库迁移 `29f3f8de3ca0` 和 `25811072c8fd` 已向 `chat_sessions` 表添加四列，但 Python 模型中**完全没有声明**：

| 数据库列 | 类型 | 迁移 | 模型映射 |
|---------|------|------|----------|
| `client_type` | VARCHAR(20), default='web' | `29f3f8de3ca0` | ❌ 缺失 |
| `project_path` | VARCHAR(500) | `29f3f8de3ca0` | ❌ 缺失 |
| `current_file` | VARCHAR(500) | `29f3f8de3ca0` | ❌ 缺失 |
| `open_files` | JSON | `25811072c8fd` | ❌ 缺失 |

**影响**: `session_context.py:44` 中 `session.client_type` 只要被执行就抛 `AttributeError`

**验证**: Python 运行时反射确认 `hasattr(ChatSession, 'client_type') == False`

**修复**: 在 `ChatSession` 类中添加四个 `Mapped` 列声明

---

### #56 — SessionContextManager 全局零调用方

**来源**: 会话逻辑专项审查 (2026-05-14) | **位置**: `backend/app/services/session_context.py`
**状态**: ⬜ 待修复

`SessionContextManager` 的三个方法在整个代码库中**零调用**：

```
全文搜索 "SessionContextManager|update_ide_context|get_ide_context" 结果：
  app/services/session_context.py:12  ← 仅类定义
  app/services/session_context.py:15  ← 仅方法定义
  app/services/session_context.py:49  ← 仅方法定义
  （无任何外部调用）
```

- `update_ide_context()` — 无人调用，IDE 上下文从未写入数据库
- `get_ide_context()` — 无人调用，构建 Agent 提示时从未读取 IDE 上下文
- `get_latest_ide_context_by_agent()` — 无人调用

**修复**: 在 LSP4J `_handle_chat_ask` 和 `_persist_lsp4j_conversation` 中集成 `SessionContextManager.update_ide_context()` 调用；在 `agent_context.py` 中集成 `get_ide_context()` 读取

---

### #57 — IDE 上下文只传递不持久化 — LSP4J 会话创建跳过持久化

**来源**: 会话逻辑专项审查 (2026-05-14) | **位置**: `jsonrpc_router.py:5667-5676` (`_persist_lsp4j_conversation`)
**状态**: ⬜ 待修复

两条写入路径均未持久化 IDE 上下文：

| 路径 | 文件 | 设置 IDE 字段? |
|------|------|---------------|
| LSP4J `_persist_lsp4j_conversation` | `jsonrpc_router.py:5667-5676` | ❌ 仅设 `id, agent_id, user_id, title, source_channel, created_at` |
| WebSocket `websocket_chat` 用户消息 | `websocket.py:502-529` | ❌ 仅设 `last_message_at` 和 `title` |

`_build_lsp4j_ide_prompt()` 从 `chat/ask` 请求参数提取的 `activeFilePath`, `sourceCode`, `filePath`, `openFiles` 等信息，只写入 LLM 系统提示后丢弃，下次请求无法复用，跨会话上下文丢失。

**修复**: 在 `_persist_lsp4j_conversation` 创建/更新会话时设置 `project_path`, `current_file`, `open_files`, `client_type="ide_plugin"`

### #64 — `GET /api/chat/{agent_id}/history` 端点 `conversation_id` 与 WebSocket 会话系统不匹配

**来源**: 会话逻辑深度审查 (2026-05-14) | **位置**: `websocket.py:126-159` ↔ `Chat.tsx:442`
**状态**: ⬜ 待修复

前端 Chat.tsx 页面加载时调用 `GET /api/chat/{id}/history`，该端点硬编码 `conversation_id = f"web_{current_user.id}"`（旧格式）。但 WebSocket 保存消息时使用的 `conversation_id` 是 `ChatSession.id`（UUID 格式）。

```python
# 旧格式 (websocket.py:133) — REST 端点查询用
conv_id = f"web_{current_user.id}"      # 例: "web_410ab5c8-..."
# 新格式 — WebSocket 保存用
conv_id = str(session.id)               # 例: "b816305b-9de5-4bd7-a44d-3b6930ea900d"
```

**后果**: 通过 WebSocket 保存的消息无法通过 REST 历史端点查询，前端页面刷新后历史消息消失。

**修复**: 重构 `/api/chat/{agent_id}/history` 按当前主会话 ID 查询，或改为带 `session_id` 参数：
```python
@router.get("/api/chat/{agent_id}/history")
async def get_chat_history(
    agent_id: uuid.UUID,
    session_id: str = Query(None),  # 新增参数
    ...
):
    conv_id = session_id or await get_primary_session_id(db, agent_id, current_user.id)
```

---

### #65 — WebSocket 连接无心跳 / 保活机制

**来源**: 会话逻辑深度审查 (2026-05-14) | **位置**: `websocket.py:162-982`
**状态**: ⬜ 待修复

WebSocket 连接建立后没有应用层 ping/pong 心跳。NAT 网关、反向代理 (nginx/haproxy)、负载均衡器通常在 60-120 秒无活动后断开空闲连接。FastAPI/Starlette/Uvicorn 不自动发送 WebSocket ping 帧。

**后果**:
- 用户长时间不发送消息后连接被悄悄断开（见日志：14:24:56 连接 → 15:15:26 重连，中间 50 分钟无日志说明连接已死但未检测到）
- 前端不知道连接已死，直到尝试发送消息才收到错误
- 与 #66 叠加后 `ConnectionManager` 中积压大量死连接

**修复**: 在 `websocket_chat` 消息循环中启动并发心跳任务：
```python
async def heartbeat_sender(websocket, interval=30):
    """每 30 秒发送应用层 ping，保持连接活跃"""
    while True:
        try:
            await asyncio.sleep(interval)
            await websocket.send_json({"type": "ping"})
        except Exception:
            break  # 连接已断开，退出心跳

# 在消息循环前创建
heartbeat_task = asyncio.create_task(heartbeat_sender(websocket))
```

参考: [RFC 6455 Section 5.5.2](https://datatracker.ietf.org/doc/html/rfc6455#section-5.5.2)、[Starlette WebSocket docs](https://www.starlette.io/websockets/)

---

### #66 — ConnectionManager 无空闲连接超时清理

**来源**: 会话逻辑深度审查 (2026-05-14) | **位置**: `websocket.py:28-96` (`ConnectionManager`)
**状态**: ⬜ 待修复

`ConnectionManager` 将每个 WebSocket 连接存入 `active_connections: dict[str, list[tuple]]`，但没有任何超时清理机制。网络中断导致客户端没有发送 close frame 时，连接对象永远停留在内存中：

```python
# 当前：只增不减（仅 onclose 触发移除）
self.active_connections[agent_id].append((websocket, session_id, user_id))
```

**后果**: 内存泄漏、达到 `MAX_CONNECTIONS=500` 后拒绝所有新连接。与 #65 叠加后加速恶化。

**修复**: 添加连接时间戳和定期清理任务：
```python
# 记录连接时间
self.active_connections[agent_id_str] = []  # WebSocket, sid, uid, last_activity_ts

# 在 lifespan 中启动后台清理任务
async def cleanup_stale_connections(self, max_idle_seconds=300):
    now = time.time()
    for agent_id in list(self.active_connections.keys()):
        self.active_connections[agent_id] = [
            (ws, sid, uid, ts) for ws, sid, uid, ts in self.active_connections[agent_id]
            if now - ts < max_idle_seconds
        ]
```

---

## 🟠 高（2 个）

### #58 — WebSocket 会话鉴权 `!= "agent"` 意外放行 `trigger` 类型会话

**来源**: 会话逻辑专项审查 (2026-05-14) | **位置**: `websocket.py:282`
**状态**: ⬜ 待修复

```python
# 当前代码：仅排除 agent 类型，trigger 也被放行
elif _existing.source_channel != "agent" and str(_existing.user_id) != str(user_id):
```

`source_channel` 可能值: `web`, `ide_lsp4j`, `feishu`, `discord`, `slack`, `agent`, `trigger`。

条件 `!= "agent"` 覆盖了 `trigger`（agent 的自我反思会话），导致 reflection 会话也跳过用户鉴权。

**修复**: 改为 `not in ("agent", "trigger")` 与 `chat_sessions.py` 其他位置保持一致

---

### #67 — `queued_messages` 被静默丢弃（用户并发消息丢失）

**来源**: 会话逻辑深度审查 (2026-05-14) | **位置**: `websocket.py:965-973`
**状态**: ⬜ 待修复

当 LLM 正在生成回复时用户发送了后续消息，这些消息被存入 `queued_messages` 列表，但在生成完成后直接丢弃：

```python
if queued_messages:
    logger.debug(
        "[WS] Discarding {} queued message(s) for session {} — "
        "re-queuing not yet implemented",
        len(queued_messages), conv_id
    )
for qm in queued_messages:
    pass  # ← 静默丢弃，用户无任何通知
```

**后果**: 用户在 AI 回复期间着急追问的消息完全丢失。用户感知为"对话丢失"或"消息被吞"。

**修复**: 至少返回错误提示 `{"type": "error", "content": "请等待当前回复完成后再发送新消息"}`；更好方案是中止当前生成并处理最新消息。

---

## 🟡 中（20 个）

### #11 — 184 处 `except Exception:`（51 个文件），约 97 处静默吞异常

**原编号**: #8 | **位置**: `backend/app/api/agents.py:884-965`、`websocket.py`、`feishu.py`、`dingtalk.py`、`discord_bot.py` 等
**状态**: 🔄 部分修复（agents.py 8处已加日志，其余 89 处待评估）

```python
try: await agent_manager.remove_container(agent)
except Exception: pass
```

> **定性**: `agent_tools.py` 的 57 处是工具加载兜底（合理保留），其余静默吞异常应至少加 `logger.warning(exc_info=True)`。

---

### #13 — OKR 模块绕过依赖注入

**原编号**: #13 | **位置**: `backend/app/api/okr.py` ~20 个端点
**状态**: 🔄 评估完成 — 需专项会话重构

OKR 路由直接创建 `async with async_session() as db:`，而项目其他模块统一 `Depends(get_db)`。导致风格不一致、嵌套 session 问题、测试困难。

**建议**: 统一改为 `db: AsyncSession = Depends(get_db)`。

---

### #21 — LSP4J 主版本升级（0.12 → 1.0.0）需端到端验证

**原编号**: #51 | **位置**: `demo-new/gradle/libs.versions.toml`

lsp4j 主版本跳跃 + `javax → jakarta` 迁移。编译通过但运行时需验证：ping、chat/answer、chat/think、tool/invoke、tool/call/sync。

---

### #23 — 390 个 `catch(Exception)` 宽泛捕获（插件侧）

**新发现**: N9 | **位置**: 插件全局
**状态**: 🔄 会话管线全部修复（14 处已修复）

插件中 390 处 `catch(Exception)`（部分来自代码生成工具），可能吞掉 `NullPointerException` 等编程错误。Chat/Core/LSP 模块优先改为具体异常类型。

---

### #24 — LanguageClientImpl 1322 行过长（30+ @Override 方法）

**新发现**: N10 | **位置**: `demo-new/.../core/lsp/LanguageClientImpl.java`
**状态**: ⬜ 待重构

单文件 1322 行、30+ 个 `@Override` 方法，违反"文件聚焦 <800 行"原则。`buildProject` → Delegate、`Completion` → 独立 Handler、工具结果同步 → 独立模块。

---

### #29 — Kotlin 仅占 ~5%（68/1361 文件），大量 POJO/Enum 可迁移

**新发现**: N15 | **位置**: 插件全局
**状态**: ⬜ 待规划

**建议**: 新文件强制 Kotlin、POJO/Enum 逐步转换。

---

### #31 — agent_tools.py 13609 行单体文件

**新发现**: N17 | **位置**: `backend/app/services/agent_tools.py`
**状态**: ⬜ 待评估

包含 100+ 个工具函数，按功能域拆分（文件操作、搜索、Agent 通信、终端）可大幅提升可维护性。

---

### #51 — ConfigMainForm.java 372 个代码问题（Qodana 最高密度文件）

**来源**: Qodana | **位置**: `src/main/java/.../ui/config/ConfigMainForm.java`
**状态**: ⬜ 待重构

Qodana 检测到 372 个问题，远超其他文件（第二名 72 个）。主要问题：魔术常量、冗余类型转换、`equals()` 表达式等。该文件 3218 行，建议拆分。

---

### #53 — 331 个魔术常量（Magic Constant）

**来源**: Qodana High | **位置**: 插件全局
**状态**: ⬜ 待评估

331 处硬编码数字/字符串（如字号、颜色值、超时毫秒数、UI 尺寸），缺乏语义化常量名。

**建议**: 低频修复——新代码强制使用常量，存量代码在修改相关逻辑时顺手提取。

---

### #59 — 会话删除时消息查询缺少 `agent_id` 过滤

**来源**: 会话逻辑专项审查 (2026-05-14) | **位置**: `chat_sessions.py:354`
**状态**: ⬜ 待修复

```python
# 当前仅按 conversation_id 删除，未加 agent_id 约束
await db.execute(sql_delete(ChatMessage).where(ChatMessage.conversation_id == str(session_id)))
```

虽然 UUID 碰撞概率极低，但缺少 `agent_id` 过滤属于防御性缺失，且无法利用 `(agent_id, conversation_id)` 联合索引加速。

**修复**: 添加 `.where(ChatMessage.agent_id == agent_id)` 条件

---

### #60 — 会话标题自动生成中 base64 图片数据残留

**来源**: 会话逻辑专项审查 (2026-05-14) | **位置**: `websocket.py:525`
**状态**: ⬜ 待修复

```python
# replace("[image_data:", "") 仅去掉了前缀标记，base64 数据体仍在字符串中
clean_title = title_src.replace("[图片] ", "📷 ").replace("[image_data:", "").strip()
_sess.title = clean_title[:40] if clean_title else content[:40]
```

当 `display_content` 为空且 `content` 为纯图片消息时，标题会是截断的 base64 片段（如 `iVBORw0KGgoAAAA...`）。虽然第528行 `[:40]` 限制了长度，但视觉效果差。

**修复**: 使用正则 `re.sub(r'\[image_data:[^\]]*\]', '', title_src)` 完整删除标记

---

### #61 — 未读计数 JOIN 使用 `cast(UUID→String)` 阻止索引利用

**来源**: 会话逻辑专项审查 (2026-05-14) | **位置**: `chat_sessions.py:111`
**状态**: ⬜ 待优化

```python
.join(ChatMessage, ChatMessage.conversation_id == cast(ChatSession.id, String))
```

`cast(UUID → String)` 使 PostgreSQL 无法使用 `ChatSession.id` 的 UUID 索引，在会话数量多时影响未读计数查询性能。

**建议**: 将 `ChatMessage.conversation_id` 改为 `UUID` 类型，或在查询前将 `session_uuid_ids` 转为字符串列表直接 `IN` 匹配

---

### #62 — 后端 ChatSession 模型与 IDE 插件 ChatSession.java 字段不对齐

**来源**: 会话逻辑专项审查 (2026-05-14) | **位置**: 后端 `models/chat_session.py` ↔ 插件 `core/lsp/model/model/ChatSession.java`
**状态**: ⬜ 待对齐

| 后端有但插件无 | 插件有但后端无 |
|---|---|
| `source_channel`, `is_primary`, `is_group`, `external_conv_id`, `peer_agent_id`, `participant_id` | `projectId`, `projectUri`, `projectName`, `sessionType` |
| `client_type`(DB), `project_path`(DB), `current_file`(DB), `open_files`(DB) | — |

插件端通过 LSP4J `chat/ask` 传递 `projectId` 等信息，但后端没有结构化存储。两端模型各自演进，未对齐。

---

### #68 — 单条 WebSocket 消息触发 5-9 次独立 DB 事务

**来源**: 会话逻辑深度审查 (2026-05-14) | **位置**: `websocket.py` 消息处理主循环
**状态**: ⬜ 待优化

每条用户消息处理创建 `async_session()` 的统计：

| 次数 | 行号 | 用途 | 能否合并 |
|------|------|------|----------|
| 1 | 419 | onboarding check | 可合并 |
| 2 | 440-450 | 模型覆盖查询 | 可合并 |
| 3 | 492-500 | 设置 onboarding 标题 | 条件触发 |
| 4 | 502-529 | 保存用户消息 + 更新 session | — |
| 5 | 565-584 | 重载模型配置 | — |
| 6 | 609-610 | onboarding phase 标记 | 条件触发 |
| 7 | 698-719 | 保存工具调用 (每条 tool 一次) | — |
| 8 | 883-890 | 更新 agent last_active_at | 可合并 |
| 9 | 942-958 | 保存 assistant 回复 | — |

**后果**: (a) 吞吐量下降，每个独立 `async_session()` 都是一次连接池获取+事务开销；(b) 如果第 9 步失败，前 8 步已提交造成不一致（如用户消息已存但助手回复丢失）。

**修复**: 将同一条消息处理链路中可共享的 DB 会话合并：配额检查+用户消息保存可共用同一 `async_session()`，agent 活跃时间更新可与 assistant 消息保存合并。

---

### #69 — LLM 错误检测依赖字符串前缀匹配（脆弱）

**来源**: 会话逻辑深度审查 (2026-05-14) | **位置**: `websocket.py:874-878`
**状态**: ⬜ 待优化

```python
_LLM_ERROR_PREFIXES = ("[LLM Error]", "[LLM call error]", "[Error]")
if not aborted and assistant_response and any(
    assistant_response.startswith(p) for p in _LLM_ERROR_PREFIXES
):
    raise RuntimeError(assistant_response)
```

**问题**: 如果 LLM 恰好返回以 `[Error]` 开头的合法文本（如代码错误说明），会被误判为调用失败。

**修复**: 在 `call_llm_with_failover` 中使用异常传播而非返回错误字符串，或在返回结构中增加 `is_error` 标志位。参考 Python Service 层的 Result 模式（`Success | Failure`）。

---

### #70 — 会话列表无分页，无法应对规模化

**来源**: 会话逻辑深度审查 (2026-05-14) | **位置**: `chat_sessions.py:63-195` (`list_sessions`)
**状态**: ⬜ 待优化

```python
result = await db.execute(
    select(ChatSession).where(...)
    .order_by(ChatSession.last_message_at.desc().nulls_last(), ...)
)
sessions = result.scalars().all()  # ← 无 limit/skip，可能返回数千条
```

**后果**: 用户量增长后单次 API 调用可能扫描并序列化数千条会话记录。FastAPI 最佳实践推荐所有列表端点支持分页。

**修复**: 添加 `skip`/`limit` 查询参数：
```python
async def list_sessions(
    ...
    skip: int = Query(0, ge=0),
    limit: int = Query(50, ge=1, le=200),
):
```

---

### #71 — ChatMessage 缺少 `(agent_id, conversation_id, created_at)` 复合索引

**来源**: 会话逻辑深度审查 (2026-05-14) | **位置**: `models/audit.py:46-64` (`ChatMessage`)
**状态**: ⬜ 待优化

当前仅有独立索引 `agent_id`, `conversation_id`, `created_at`，但最核心的查询模式是最频繁的：

```sql
-- WebSocket 历史加载 (websocket.py:314-319)
SELECT * FROM chat_messages
WHERE agent_id = ? AND conversation_id = ?  -- 两列条件
ORDER BY created_at DESC LIMIT ?            -- 排序列
```

PostgreSQL 可以使用 Bitmap Index Scan 合并两个独立索引，但复合索引效率更高且减少 I/O。

**修复**: 在 `ChatMessage.__table_args__` 添加：
```python
Index("ix_chat_messages_agent_conv_created", "agent_id", "conversation_id", "created_at"),
```

---

### #72 — 会话上下文截断可能拆分 tool_call 对

**来源**: 会话逻辑深度审查 (2026-05-14) | **位置**: `websocket.py:782-784`
**状态**: ⬜ 待优化

```python
_truncated = conversation[-ctx_size:]           # 只保留最后 ctx_size 条
while _truncated and _truncated[0].get("role") == "tool":
    _truncated.pop(0)                            # 仅移除前导 tool 消息
```

OpenAI API 要求每条 `role=tool` 消息前必须有对应的 `assistant(tool_calls)` 消息。当前代码只保证不移除以 `tool` 开头的片段，但未保证移除后不产生孤立的 `tool` 消息（如截断在 assistant 消息之后、tool 结果之前）。

**修复**: 从后往前扫描，确保每个 `tool` 消息有对应的前导 `assistant + tool_calls`：
```python
# 从后往前构建，遇到孤立的 tool 消息就跳过
safe = []
seen_tool_call_ids = set()
for msg in reversed(_truncated):
    if msg.get("role") == "tool":
        tc_id = msg.get("tool_call_id")
        if tc_id not in seen_tool_call_ids:
            continue  # 孤立的 tool 消息，跳过
    elif msg.get("role") == "assistant" and msg.get("tool_calls"):
        for tc in msg["tool_calls"]:
            seen_tool_call_ids.add(tc["id"])
    safe.append(msg)
_truncated = list(reversed(safe))
```

---

### #73 — `websocket.py` 982 行单体文件，职责过载

**来源**: 会话逻辑深度审查 (2026-05-14) | **位置**: `backend/app/api/websocket.py`
**状态**: ⬜ 待重构

单个文件混合了：`ConnectionManager` (连接管理)、token 认证、agent/model 加载、历史消息加载与转换、onboarding 触发器、配额检查、LLM 调用、工具调用持久化、Live Preview 注入、workspace 活动通知、任务创建、协程取消管理。

**修复方向**: 按职责拆分——
- `ConnectionManager` → `app/services/connection_manager.py`（已分析心跳/超时需整合）
- 会话加载逻辑 → `app/services/chat_session_service.py`（已有部分）
- LLM 调用封装 + abort 管理 → 独立 `app/services/llm_stream_handler.py`
- WebSocket 路由保持瘦层（~200 行），仅做参数解析和调度

---

### #74 — IDE 插件 WebSocket URL 未传递 `session_id` 参数

**来源**: 会话逻辑深度审查 (2026-05-14) | **位置**: `ClawithSettingsState.java:186`
**状态**: ⬜ 待修复

```java
// 当前：仅有 agent_id，无 session_id
return baseUrl + CLAWITH_WS_PATH + "?agent_id=" + agentId;
// 期望: 还应有 &session_id=xxx
```

IDE 插件通过 LSP4J 连接后端 WebSocket（`/api/plugins/clawith-lsp4j/ws`），但 URL 中缺少 `session_id` 参数。虽然 LSP4J 插件内部有独立的会话管理，但缺失此参数导致：
- 后端无法将 IDE 侧的会话与 `ChatSession` 模型关联
- 无法利用后端的会话历史恢复和未读计数功能
- IDE 端重新连接时无法无缝恢复之前的对话上下文

**修复**: 在 `buildClawithWebSocketUrl` 中添加 `session_id` 参数，由 IDE 端维护并在重连时传递。

---

## 🟢 低（13 个）

### 后端（6 个）

| # | 问题 | 原编号 | 位置 | 建议 |
|----|------|--------|------|------|
| 34 | `list_tasks()` 12 个参数 | #21 | `api/tasks.py` | 封装为 Pydantic Model 会改变 OpenAPI Schema，暂缓 |
| 36 | 无错误追踪集成 | #25 | 后端整体 | 可选 Sentry SDK |
| 37 | Docker SYS_ADMIN + docker.sock | #27 | `docker-compose.yml` | 非沙箱场景移除权限，考虑 rootless |
| 63 | 多处文件内重复导入 | 新增 | `websocket.py`, `chat_sessions.py`, `jsonrpc_router.py` | 统一在文件顶部导入，避免函数体内重复 `from ... import ...` |
| 77 | ChatSession 模型无软删除和状态字段 | 会话深度审查 | `models/chat_session.py` | 添加 `deleted_at`(软删除)、`status`(active/archived)、`message_count`(缓存计数) |
| 78 | 日志级别不一致——高频 INFO 淹没关键信息 | 会话深度审查 | `websocket.py:387,530,960` | "Waiting for message"→DEBUG、"message saved"→DEBUG，保留 error/warning 在 INFO |

### IDEA 插件（7 个）

| # | 问题 | 原编号 | 位置 | 建议 |
|----|------|--------|------|------|
| 44 | 76 个工具类分散 | #22 | `util/` 包 | 按功能合并（大型重构） |
| 45 | `model/model` 路径嵌套 | #26 | `core/lsp/model/model/` | 重构为 `core/lsp/model/`（大型重构） |
| 47 | flexmark/swingx 未维护 | #45 | `libs.versions.toml` | 标记为传递依赖，跟踪替代方案 |
| 49 | 9 个 ChatXxxProcessor 重复模式 | #69 | `chat/processor/` | 提取 `AbstractChatProcessor` 基类 |
| 54 | Qodana 样式问题（1234 处 equals→Objects.equals + 821 冗余转型 + 462 模式变量 + 352 final 字段等） | Qodana | 插件全局 | 大批量低优先级，可在日常迭代中逐步修复 |
| 75 | ChatSessionUtil 全局可变 Map 存储会话状态（request_id→sessionType） | 会话深度审查 | `ChatSessionUtil.java:252-269` | 迁移到 `UserData`/`PropertiesComponent` 持久化，避免内存泄漏和重启丢失 |
| 76 | IDE 插件日志缺少 ChatSession.id 关联，无法端到端追踪 | 会话深度审查 | `CosyWebSocketConnectClient.java` | 连接/断开/错误日志增加后端 `session_id` 字段 |

---

## 汇总

| # | 问题 | 严重程度 | 来源 | 状态 |
|---|------|----------|------|------|
| 1 | 智能体看不到本地项目（根因见 #55/#56/#57） | 🔴 | 人工评审 | 🔄 需端到端验证 |
| 55 | ChatSession 模型缺少 4 个 IDE 上下文字段 | 🔴 | 会话专项审查 | ⬜ |
| 56 | SessionContextManager 全局零调用方 | 🔴 | 会话专项审查 | ⬜ |
| 57 | IDE 上下文只传递不持久化 | 🔴 | 会话专项审查 | ⬜ |
| 64 | GET /api/chat/history conversation_id 与 WebSocket 不匹配 | 🔴 | 会话深度审查 | ⬜ |
| 65 | WebSocket 无心跳/保活机制 | 🔴 | 会话深度审查 | ⬜ |
| 66 | ConnectionManager 无空闲连接超时清理 | 🔴 | 会话深度审查 | ⬜ |
| 58 | WebSocket 鉴权 `!= "agent"` 放行 `trigger` | 🟠 | 会话专项审查 | ⬜ |
| 67 | queued_messages 被静默丢弃（并发消息丢失） | 🟠 | 会话深度审查 | ⬜ |
| 11 | 97处静默异常 | 🟡 | 人工评审 | 🔄 部分修复 |
| 13 | OKR绕过DI | 🟡 | 人工评审 | 🔄 需专项会话 |
| 21 | LSP4J 1.0 端到端验证 | 🟡 | 人工评审 | ⬜ |
| 23 | 390 个 catch(Exception) 宽泛（插件） | 🟡 | 人工评审 | 🔄 会话管线全修复 |
| 24 | LanguageClientImpl 1322 行过长 | 🟡 | 人工评审 | ⬜ |
| 29 | Kotlin 仅占 5% | 🟡 | 人工评审 | ⬜ |
| 31 | agent_tools.py 13609 行单体文件 | 🟡 | 人工评审 | ⬜ |
| 51 | ConfigMainForm.java 372 个问题（3218 行） | 🟡 | Qodana | ⬜ |
| 53 | 331 个魔术常量 | 🟡 | Qodana | ⬜ |
| 59 | 会话删除消息查询缺少 agent_id 过滤 | 🟡 | 会话专项审查 | ⬜ |
| 60 | 标题生成 base64 残留 | 🟡 | 会话专项审查 | ⬜ |
| 61 | 未读查询 cast(UUID→String) 阻止索引 | 🟡 | 会话专项审查 | ⬜ |
| 62 | 前后端 ChatSession 模型不对齐 | 🟡 | 会话专项审查 | ⬜ |
| 68 | 单条 WebSocket 消息 5-9 次 DB 事务 | 🟡 | 会话深度审查 | ⬜ |
| 69 | LLM 错误检测依赖字符串前缀匹配 | 🟡 | 会话深度审查 | ⬜ |
| 70 | 会话列表无分页 | 🟡 | 会话深度审查 | ⬜ |
| 71 | ChatMessage 缺 (agent_id, conv_id, created_at) 复合索引 | 🟡 | 会话深度审查 | ⬜ |
| 72 | 会话上下文截断可能拆分 tool_call 对 | 🟡 | 会话深度审查 | ⬜ |
| 73 | websocket.py 982 行单体文件职责过载 | 🟡 | 会话深度审查 | ⬜ |
| 74 | IDE 插件 WebSocket URL 未传递 session_id | 🟡 | 会话深度审查 | ⬜ |
| 34 | 参数过多 | 🟢 | 人工评审 | 🔄 暂缓 |
| 36 | 无错误追踪 | 🟢 | 人工评审 | ⬜ |
| 37 | Docker权限过大 | 🟢 | 人工评审 | ⬜ |
| 63 | 多处重复导入 | 🟢 | 会话专项审查 | ⬜ |
| 77 | ChatSession 无软删除和状态字段 | 🟢 | 会话深度审查 | ⬜ |
| 78 | 日志级别不一致 | 🟢 | 会话深度审查 | ⬜ |
| 44 | 工具类分散 | 🟢 | 人工评审 | ⬜ |
| 45 | model/model嵌套 | 🟢 | 人工评审 | ⬜ |
| 47 | flexmark/swingx 未维护 | 🟢 | 人工评审 | ⬜ |
| 49 | 9 个 Processor 重复模式 | 🟢 | 人工评审 | ⬜ |
| 54 | Qodana 样式问题（批量低优先级） | 🟢 | Qodana | ⬜ |
| 75 | ChatSessionUtil 全局 Map 存储会话状态 | 🟢 | 会话深度审查 | ⬜ |
| 76 | IDE 日志缺 ChatSession.id 关联 | 🟢 | 会话深度审查 | ⬜ |

## 🟡 插件 UI 优化（4 个，2026-05-14）

| # | 问题 | 严重度 | 位置 | 方案 |
|---|------|--------|------|------|
| 79 | 设置页登录后不显示当前智能体名 | 🟢 | `ClawithConfigurable.java` `buildLoggedInCard()` | 增加 `agentNameLabel`，`applyStateToUi()` 同步 `"当前智能体: {name}"` |
| 80 | 聊天下拉智能体无角色描述 | 🟢 | `ChatModeItem.java` `getTooltip()` | `fromClawithAgent()` 增加 `roleDescription`，tooltip 显示 `name + "\n" + roleDesc` |
| 81 | JWT 过期无用户通知 | 🟡 | `CosyWebSocketConnectClient.java` `onClose()` | 4001/4002 时弹出 Notification 告知重新登录 |
| 82 | URL 输入框无 placeholder | 🟢 | `ClawithConfigurable.java` `createComponent()` | `putClientProperty("JTextField.placeholderText", "http://localhost:8008")` |

## 🔴 会话流畅度瓶颈（12 个，2026-05-14 第 10 轮）

| # | 瓶颈 | 严重度 | 位置 | 延迟量 | 修复方向 |
|---|------|--------|------|--------|----------|
| 83 | 30s 硬超时误杀长工具会话 | 🔴 | `BaseChatPanel.java:1335` | 轮次失败 | 超时改为累计工具轮次感知，或提高到 120s |
| 84 | 上下文跨轮次无界增长 | 🔴 | `caller.py:592` 循环 | +50-200% LLM 推理 | 增加轮次上限后上下文摘要/截断 |
| 85 | 每工具调用 4-5 条 WebSocket 通知 | 🟠 | `jsonrpc_router.py:1877-1985` | +80-200ms/工具 | 合并 toolCall+sync 为单条消息 |
| 86 | `chat/finish` 前强制 200ms sleep | 🟠 | `jsonrpc_router.py:2356` | +200ms/轮 | 修复 IDE 竞态后移除 sleep |
| 87 | `future.get()` 同步阻塞后台线程 | 🟡 | `LanguageWebSocketService.java:329` | 线程阻塞整轮 | 改用 CompletableFuture 异步回调 |
| 88 | 工具上下文 Java+Python 双重注入 | 🟡 | `BaseChatPanel.java:2170` + `jsonrpc_router.py:1633` | 浪费 token | 去掉 Java 侧注入，仅保留 Python 侧 |
| 89 | 写工具串行阻塞整轮 | 🟡 | `caller.py:793` | 所有写工具延迟累加 | 同文件无依赖写工具可并行 |
| 90 | `toolCallId` 队列失配产生僵尸卡片 | 🟡 | `jsonrpc_router.py:3042-3063` | UI 卡死 | 加队列顺序校验 + 回退匹配 |
| 91 | 冷缓存 DB 历史加载阻塞 LLM | 🟡 | `jsonrpc_router.py:1601-1624` | +50-500ms | 延长缓存 TTL + 异步预加载 |
| 92 | 每轮强制快照同步 | 🟡 | `jsonrpc_router.py:1544-1549` | +100-500ms/轮 | 仅文件变更时同步 |
| 93 | `FINISH_PROTOCOL_REMINDER` 额外 LLM 轮 | 🟡 | `caller.py:700` | +5-15s | 文本无工具时自动视为 finish |
| 94 | `_UI_HEAVY_SEARCH_TOOLS` 卡片闪烁 | 🟢 | `jsonrpc_router.py:1896-1909` | UI 抖动 | 快速工具延迟卡片创建，结果直接展示 |

> **已修复并剔除（56 个）**：原 #2, #3, #4, #7, #10, #12, #14, #15, #17, #18, #26, #32, #33, #42, #51, #53, #57-#68, L1, L2, L4, L5 + #46（SLF4J 桥接）、#50（2 个无效 i18n 键）、#52（2 个弃用 API 迁移）、#56（tomcat 版本孤岛误报）、#57（libs.versions.toml 注释依赖）、#22（.zshrc 冲突已消失）、#27（CORS 已正确配置）、#16（print→logger）、#28（UserLogin max_length）、#35（结构化 JSON 日志）、#38（execute_code sync 警告优化）、#39（duckduckgo_search 网络超时已确认一次性波动）、#40（websocket.py 冗余 import）、#41（alembic mergepoint 延后处理）、#43（合并至 #33）、#12（diff 算法死代码删除 14 文件 ~573 行）

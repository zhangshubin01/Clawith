# Clawith 后端 + IDEA 插件 全量代码评审 — 问题清单

> 评审时间：2026-05-12 | 更新：2026-05-15（第 15 轮，三端日志深度交叉分析——LSP4J ↔ WebUI 推送断裂 / 双重事务 / 跨层追踪缺失）
> 评审范围：Clawith 后端 `backend/` (365 个 Python 文件) + IDEA 插件 `demo-new/` (1361 个 Kotlin/Java 文件)

**共 47 个待处理问题**：🔴0　🟠2　🟡20　🟢22　💡3

> 本回合修复 15 项：#1 本地项目上下文 ✅ | #84 上下文无界增长 ✅ | #93 FINISH_PROTOCOL_REMINDER ✅ | #85 WS 通知冗余 ✅ | #11 静默异常 ✅ | #24 LanguageClientImpl 重构 ✅ | #61 cast UUID 索引 ✅ | #68 DB 事务合并 ✅ | #69 LLM 错误前缀 ✅ | #71 复合索引 ✅ | #72 tool_call 截断 ✅ | #89 写工具并行 ✅ | #91 冷缓存预加载 ✅ | #92 增量快照 ✅ | #96 品牌统一 ✅

---

## 🟠 高（2 个）

### #97 — ChatToolEventProcessor 轮询等待日志风暴

**来源**: 会话三端日志分析 (2026-05-14) | **位置**: `ChatToolEventProcessor.java:124-145`
**状态**: ⬜ 待修复

同一 requestId (`d043cfa2`) 对 11+ 个 toolCallId 每秒重试 1 次，持续 60 秒。单次会话产生 400+ 条 WARN 日志：
```
[ChatToolEventProcessor][consumer] wait toolPanel again for request: d043cfa2..., panel: b9a70b55...
```
**根因**: 工具调用事件（`tool/call/sync`）先于 ToolPanel UI 组件注册到达。事件消费线程使用 `Condition.await(1, SECONDS)` 轮询，无指数退避。
**修复**: 添加指数退避（1s→2s→4s→8s→fail），最多重试 4 次；增加 ToolPanel 预注册机制（工具调用发出前预留占位面板）。

---

### #110 — Terminal 工具启动超时堆积导致插件端整体超时

**来源**: 会话三端日志分析 (2026-05-14) | **位置**: `ToolPanel.java` terminal startup timeout
**状态**: ⬜ 待修复

LLM 在一轮中连续调用 `run_in_terminal` 7 次（编译、测试、构建等），7 个终端工具在同一毫秒超时（日志证据）：
```
21:54:20 WARN - waiting for terminal startup timeout ×7 (同时)
21:54:57 PLUGIN CHAT TIMEOUT DETECTED: actualWaitTime=52892ms (52s)
```
随后插件端 180s 超时触发，后端结果无法送达。

**修复**: 终端工具增加启动超时退避（每次失败后超时翻倍，最大 30s）；插件端检测到工具执行中时动态延长超时计时器。

---

## 🟡 中（20 个）

### #13 — OKR 模块绕过依赖注入

**原编号**: #13 | **位置**: `backend/app/api/okr.py` ~20 个端点
**状态**: 🔄 评估完成 — 需专项会话重构

OKR 路由直接创建 `async with async_session() as db:`，而项目其他模块统一 `Depends(get_db)`。

**建议**: 统一改为 `db: AsyncSession = Depends(get_db)`。

---

### #21 — LSP4J 主版本升级（0.12 → 1.0.0）需端到端验证

**原编号**: #51 | **位置**: `demo-new/gradle/libs.versions.toml`

lsp4j 主版本跳跃 + `javax → jakarta` 迁移。需验证 ping、chat/answer、chat/think、tool/invoke、tool/call/sync。

---

### #23 — 390 个 `catch(Exception)` 宽泛捕获（插件侧）

**新发现**: N9 | **位置**: 插件全局
**状态**: 🔄 会话管线全部修复（14 处已修复）

Chat/Core/LSP 模块优先改为具体异常类型。

---

### #29 — Kotlin 仅占 ~5%（68/1361 文件），大量 POJO/Enum 可迁移

**新发现**: N15 | **位置**: 插件全局
**状态**: ⬜ 待规划

新文件强制 Kotlin、POJO/Enum 逐步转换。

---

### #31 — agent_tools.py 13609 行单体文件

**新发现**: N17 | **位置**: `backend/app/services/agent_tools.py`
**状态**: ⬜ 待评估

按功能域拆分（文件操作、搜索、Agent 通信、终端）可大幅提升可维护性。

---

### #51 — ConfigMainForm.java 372 个代码问题（Qodana 最高密度文件）

**来源**: Qodana | **位置**: `src/main/java/.../ui/config/ConfigMainForm.java`
**状态**: ⬜ 待重构

该文件 3218 行，建议拆分。

---

### #53 — 331 个魔术常量（Magic Constant）

**来源**: Qodana High | **位置**: 插件全局
**状态**: ⬜ 待评估

新代码强制使用常量，存量代码在修改相关逻辑时顺手提取。

---

### #70 — 会话列表无分页，无法应对规模化

**来源**: 会话逻辑深度审查 (2026-05-14) | **位置**: `chat_sessions.py:63-195`
**状态**: ⬜ 待优化

`result.scalars().all()` 无 `limit/skip`，可能返回数千条。

**修复**: 添加 `skip`/`limit` 查询参数。

---

### #73 — `websocket.py` 982 行单体文件，职责过载

**来源**: 会话逻辑深度审查 (2026-05-14) | **位置**: `backend/app/api/websocket.py`
**状态**: ⬜ 待重构

- `ConnectionManager` → `app/services/connection_manager.py`
- 会话加载逻辑 → `app/services/chat_session_service.py`
- LLM 调用封装 → `app/services/llm_stream_handler.py`

---

### #98 — ConnectionManager 使用裸元组 + O(n) 操作

**来源**: 会话三端日志分析 (2026-05-14) | **位置**: `backend/app/api/websocket.py:40-78`
**状态**: ⬜ 待重构

当前使用裸元组存储连接状态，通过索引解包（`t[0]`, `t[1]`），极易出错且难维护：
```python
self.active_connections: dict[str, list[tuple]] = {}
# agent_id -> [(websocket, session_id, user_id, connected_at, last_activity)]
```
`touch()` 和 `disconnect()` 遍历整个列表查找匹配连接，O(n) 复杂度。
**修复**: 定义 `@dataclass` 替代元组；`touch()` 改用 `{websocket_hash: index}` 索引加速查找；增加连接级别的日志标识。

---

### #99 — WebSocket chat endpoint 不设置 client_type，跨端无法区分

**来源**: 会话三端日志分析 (2026-05-14) | **位置**: `backend/app/api/websocket.py:232-410`
**状态**: ⬜ 待修复

WebSocket chat endpoint 创建/使用 ChatSession 时从不设置 `client_type` 字段，始终使用 DB 默认值 `"web"`。而 LSP4J 路径正确设置了 `client_type="ide_lsp4j"`（`jsonrpc_router.py:5774`）。导致 WebUI 无法区分 Web 端和 IDE 端会话。
**修复**: WebSocket 连接时根据连接来源（URL 参数或额外 Header）设置 `client_type`；为 IDE 插件直连场景预留 `client_type="ide_plugin"`。

---

### #100 — chat_session_service.py 只处理 web 渠道，IDE 会话无法获得主会话管理

**来源**: 会话三端日志分析 (2026-05-14) | **位置**: `backend/app/services/chat_session_service.py:27-33`
**状态**: ⬜ 待修复

`get_primary_platform_session()` 和 `ensure_primary_platform_session()` 硬编码 `source_channel == "web"`，IDE LSP4J (`ide_lsp4j`) 会话无法获得 `is_primary` 管理和懒选举能力。
**修复**: 将 `source_channel` 改为参数传入，或扩展为支持多 channel 的主会话管理；至少覆盖 `"web"` 和 `"ide_lsp4j"` 两种渠道。

---

### #101 — LSP4J 并发 chat/ask 被直接拒绝而非排队

**来源**: 会话三端日志分析 (2026-05-14) | **位置**: `jsonrpc_router.py:1525-1533`
**状态**: ⬜ 待修复

当用户快速连续发送两条消息时，第二条被直接返回 `-32602 "Another chat is in progress"`。IDE 插件端无重试逻辑，消息丢失。
**修复**: 使用 `asyncio.Queue` 实现单请求排队（最多排队 1 个），超出则拒绝；排队消息在前一个完成后自动消费。

---

### #102 — `_SESSION_MESSAGE_CACHE` 模块级字典无大小上限

**来源**: 会话三端日志分析 (2026-05-14) | **位置**: `jsonrpc_router.py:2316` 及附近
**状态**: ⬜ 待修复

每个 `(user_id, session_id)` 缓存完整消息历史，无 LRU 淘汰机制。长时间运行后内存持续增长。
**修复**: 添加 `maxsize=500` LRU 限制（使用 `collections.OrderedDict` 或 `lru_cache` 模式）；或统一使用 `current_lsp4j_message_history` ContextVar 单例缓存，模块级缓存仅作只读回退。

---

### #103 — 每轮消息都重新加载模型配置

**来源**: 会话三端日志分析 (2026-05-14) | **位置**: `backend/app/api/websocket.py:638-657`
**状态**: ⬜ 待优化

每收到一条用户消息就执行 DB 查询重新加载 `Agent + LLMModel`，但模型配置一般不频繁变化。浪费 DB 连接和响应延迟。
**修复**: 添加 TTL 缓存（60s），仅在模型配置可能变更时（如 Settings 更新事件）主动失效缓存；或使用后台定时刷新。

---

### #104 — 连接空闲清理 ping 后静默吞异常，且成功 ping 后重置 timer 可能导致半死连接永不被清理

**来源**: 会话三端日志分析 (2026-05-14) | **位置**: `backend/app/api/websocket.py:80-105`
**状态**: ⬜ 待修复

```python
try:
    await ws.send_json({"type": "ping"})
    alive.append((ws, sid, uid, cat, _now))  # 成功 ping → 重置 last_activity
except Exception:
    logger.info("[WS] 清理死连接: ...")  # 无异常详情
```
成功 ping 后直接重置 `last_activity`，如果客户端响应 ping 但实际已无消息交互，连接将永远不被清理。异常时只记录 info 级别日志，无实际异常信息。
**修复**: 区分 ping 成功和真实消息活动（用两个时间戳）；记录异常类型和消息到日志；连续 N 次仅 ping 无消息则主动断连。

---

### #111 — DeepSeek reasoning_content 心跳请求格式错误

**来源**: 会话三端日志分析 (2026-05-14) | **位置**: `backend/app/services/heartbeat.py:319`
**状态**: ⬜ 待修复

心跳（heartbeat）请求未携带 `reasoning_content`，DeepSeek thinking mode 要求回传前轮 reasoning 历史，连续 6 次返回 HTTP 400：
```
"The `reasoning_content` in the thinking mode must be passed back to the API."
```

**影响**: 心跳检查失败（不影响用户对话，但监控指标不准确）。

**修复**: 心跳请求中添加占位 `reasoning_content` 或跳过 thinking mode 参数。

---

### #105 — IDE 插件会话代码全部使用 Java，违反 Kotlin 优先原则

**来源**: 会话三端日志分析 (2026-05-14) | **位置**: `ChatToolEventProcessor.java` (300+ 行)、`ChatSessionUtil.java` (270 行)、`CosyWebSocketConnectClient.java` 等
**状态**: ⬜ 待迁移

核心会话管线文件全部为 Java，而 CLAUDE.md 明确要求 "IDE 插件代码优先用 Kotlin"。与 #29（Kotlin 仅占 5%）相关联，但更聚焦于会话模块。
**修复**: 新会话功能强制 Kotlin；`ChatToolEventProcessor`、`ChatSessionUtil` 作为迁移优先级最高的两个文件。

---

### #114 — LSP4J `asyncio.create_task` 集合在异常场景下可能无界增长

**来源**: 会话三端交叉分析 (2026-05-15) | **位置**: `router.py:204-209` (`_ws_tasks: set[asyncio.Task]`)
**状态**: ⬜ 待修复

LSP4J 消息循环中每条消息用 `asyncio.create_task()` 创建任务并加入 `_ws_tasks` set，完成时通过 `add_done_callback(_ws_tasks.discard)` 移除：

```python
_ws_tasks: set[asyncio.Task] = set()
while True:
    raw_data = await websocket.receive_text()
    t = asyncio.create_task(jsonrpc.route(raw_data))
    _ws_tasks.add(t)
    t.add_done_callback(_ws_tasks.discard)  # 仅正常完成时回调
```

**问题**: 如果 Task 被 `CancelledError` 中断但未正常 done（如 cleanup 中 `keepalive_task.cancel()` 后 await），`discard` 回调不会触发，task 永远存留在 set 中。此外，`route()` 方法在处理某些非法消息时可能永久挂起。

**修复**: 添加 `_ws_tasks` 定期清理（>600s 的 task 强制移除）；或在 finally block 中注册 done callback。

---

### #115 — LSP4J `_persist_lsp4j_chat_turn` 双重事务管理冗余嵌套

**来源**: 会话三端交叉分析 (2026-05-15) | **位置**: `jsonrpc_router.py:5751-5760`
**状态**: ⬜ 待简化

当前代码使用了**两层嵌套**的事务管理：

```python
# 第 1165 行：外层 async_session() — 创建连接 + 隐式事务
async with async_session() as db:
    # 第 1174 行：内层 db.begin() — 显式子事务
    async with db.begin():
        sr = await db.execute(...)
        sess = sr.scalar_one_or_none()
    # 第 1179 行：事务块结束，ChatSession 已提交
    # → 但后续 ChatMessage 操作仍在 async_session() 上下文中
```

外层 `async_session()` 已提供事务上下文，内层 `db.begin()` 创建的是嵌套子事务（SAVEPOINT）。但 `db.begin()` 块结束后（line 5764），外层的 `async_session()` 上下文还持续到 `ChatMessage` 插入（line 5817-5838）。两个事务块导致 ChatSession 变更先提交、ChatMessage 后提交，形成两个原子单位。

**修复**: 去掉内层 `async with db.begin():`，所有操作统一在外层 `async_session()` 中完成，由 `session.commit()` 统一原子性保证。

同样适用于 `_persist_lsp4j_tool_call` (line 5905+5917)。

---

## 🟢 低（22 个）

### 后端（6 个）

| # | 问题 | 位置 | 建议 |
|----|------|------|------|
| 34 | `list_tasks()` 12 个参数 | `api/tasks.py` | 封装为 Pydantic Model，暂缓 |
| 36 | 无错误追踪集成 | 后端整体 | 可选 Sentry SDK |
| 37 | Docker SYS_ADMIN + docker.sock | `docker-compose.yml` | 非沙箱场景移除权限 |
| 63 | 多处文件内重复导入 | `websocket.py` 等 | 统一在文件顶部导入 |
| 77 | ChatSession 无软删除和状态字段 | `models/chat_session.py` | 添加 `deleted_at`, `status`, `message_count` |
| 78 | 日志级别不一致——高频 INFO 淹没关键信息 | `websocket.py` | 改为 DEBUG 级 |

### IDEA 插件（5 个）

| # | 问题 | 位置 | 建议 |
|----|------|------|------|
| 44 | 76 个工具类分散 | `util/` 包 | 按功能合并 |
| 45 | `model/model` 路径嵌套 | `core/lsp/model/model/` | 重构为 `core/lsp/model/` |
| 47 | flexmark/swingx 未维护 | `libs.versions.toml` | 跟踪替代方案 |
| 49 | 9 个 ChatXxxProcessor 重复模式 | `chat/processor/` | 提取基类 |
| 54 | Qodana 样式问题（大批量） | 插件全局 | 日常迭代逐步修复 |

### 会话分析新增（8 个）

| # | 问题 | 位置 | 建议 |
|----|------|------|------|
| 106 | `CosyWebSocketConnectClient` ThreadLocal API Key 潜在泄漏 | `CosyWebSocketConnectClient.java:47` | 重连时清除旧 ThreadLocal 值；改为构造参数传递，避免 ThreadLocal |
| 107 | `ChatSession` Java 模型 toString 不含新增字段 | `ChatSession.java:266-290` | 补充 `sourceChannel`、`clientType`、`isPrimary` 到 toString |
| 108 | `ConnectionManager.connect()` 未检查同一 session 已有连接 | `websocket.py:48-61` | 记录同一 session 的连接数；发送消息时去重避免重复投递 |
| 112 | 流式/最终回复覆盖不匹配（answer_sync overwrite_mismatch） | `jsonrpc_router.py:2353` | 确保最终回复为流式输出完整版（当前已有保护逻辑，低优先级） |
| 113 | toolCallId Future 累积导致无匹配（pending_count=11） | `jsonrpc_router.py:3288` | 增加 `_tool_futures` 超时清理（>300s 自动移除） |
| 116 | LSP4J pusher 通知 WebUI 用的 session UUID 始终不同于 WebUI 当前会话 UUID | `jsonrpc_router.py:5854-5857` | IDE UUID ≠ WebUI UUID，push 通知永远无人接收；改为按 agent_id 广播或从 session 查询活跃 WebUI 连接 |
| 117 | 三端日志格式不统一（后端 ` trace_id  ` vs 插件 `#class`），缺跨层关联 ID | 后端 `logging_config.py` ↔ 插件 `Logger.getInstance()` | 统一使用 `X-Trace-Id` 或 `sessionId` 作为跨层关联键 |
| 118 | IDE 插件 `[Clawith]` 日志前缀使用不一致（部分有、部分无） | `ChatToolEventProcessor` vs `LanguageClientImpl` | 建议在 `CosyLoggerUtil` 中统一格式化，强制所有 `[Clawith]` 日志加前缀 |

### 插件 UI 优化（3 个）

| # | 问题 | 位置 | 方案 |
|---|------|------|------|
| 79 | 设置页登录后不显示当前智能体名 | `ClawithConfigurable.java` | 增加 `agentNameLabel` |
| 80 | 聊天下拉智能体无角色描述 | `ChatModeItem.java` | tooltip 显示 `name + "\n" + roleDesc` |
| 82 | URL 输入框无 placeholder | `ClawithConfigurable.java` | 添加 placeholder 提示 |

---

## 💡 低风险 / 流畅度备注（3 个）

| # | 瓶颈 | 位置 | 说明 |
|---|------|------|------|
| 75 | ChatSessionUtil 全局 Map 存储 request_id→sessionType | `ChatSessionUtil.java:252-269` | 已使用 IntelliJ UserData，低风险保留 |
| 94 | `_UI_HEAVY_SEARCH_TOOLS` 卡片闪烁 | `jsonrpc_router.py:1896-1909` | 快速工具延迟卡片创建即可消除 |
| 109 | IDE 插件无 session 恢复机制 | `CosyWebSocketConnectClient` + `LanguageClientImpl` | WebSocket 重连后会话状态全部丢失；需专项设计 session 快照/恢复协议 |

---

## 汇总

| # | 问题 | 严重度 | 来源 | 状态 |
|---|------|----------|------|------|
| 97 | ChatToolEventProcessor 日志风暴 | 🟠 | 会话日志分析 | ⬜ |
| 110 | Terminal 超时堆积 | 🟠 | 会话日志分析 | ⬜ |
| 13 | OKR绕过DI | 🟡 | 人工评审 | 🔄 需专项会话 |
| 21 | LSP4J 1.0 端到端验证 | 🟡 | 人工评审 | ⬜ |
| 23 | 390 个 catch(Exception)（插件） | 🟡 | 人工评审 | 🔄 会话管线全修复 |
| 29 | Kotlin 仅占 5% | 🟡 | 人工评审 | ⬜ |
| 31 | agent_tools.py 13609 行 | 🟡 | 人工评审 | ⬜ |
| 51 | ConfigMainForm.java 372问题 | 🟡 | Qodana | ⬜ |
| 53 | 331 个魔术常量 | 🟡 | Qodana | ⬜ |
| 70 | 会话列表无分页 | 🟡 | 会话深度审查 | ⬜ |
| 73 | websocket.py 982行单体 | 🟡 | 会话深度审查 | ⬜ |
| 98 | ConnectionManager 裸元组+O(n) | 🟡 | 会话日志分析 | ⬜ |
| 99 | WebSocket 不设置 client_type | 🟡 | 会话日志分析 | ⬜ |
| 100 | chat_session_service 只处理 web | 🟡 | 会话日志分析 | ⬜ |
| 101 | LSP4J 并发 chat/ask 直接拒绝 | 🟡 | 会话日志分析 | ⬜ |
| 102 | _SESSION_MESSAGE_CACHE 无界增长 | 🟡 | 会话日志分析 | ⬜ |
| 103 | 每轮消息重新加载模型配置 | 🟡 | 会话日志分析 | ⬜ |
| 104 | 连接空闲清理吞异常 | 🟡 | 会话日志分析 | ⬜ |
| 105 | IDE 会话代码全 Java | 🟡 | 会话日志分析 | ⬜ |
| 111 | reasoning_content 心跳错误 | 🟡 | 会话日志分析 | ⬜ |
| 114 | LSP4J create_task 无界增长 | 🟡 | 会话交叉分析 | ⬜ |
| 115 | LSP4J 双重事务嵌套 | 🟡 | 会话交叉分析 | ⬜ |
| 34 | 参数过多 | 🟢 | 人工评审 | 🔄 暂缓 |
| 36 | 无错误追踪 | 🟢 | 人工评审 | ⬜ |
| 37 | Docker权限过大 | 🟢 | 人工评审 | ⬜ |
| 63 | 多处重复导入 | 🟢 | 会话专项审查 | ⬜ |
| 77 | ChatSession 无软删除 | 🟢 | 会话深度审查 | ⬜ |
| 78 | 日志级别不一致 | 🟢 | 会话深度审查 | ⬜ |
| 44 | 工具类分散 | 🟢 | 人工评审 | ⬜ |
| 45 | model/model嵌套 | 🟢 | 人工评审 | ⬜ |
| 47 | flexmark/swingx 未维护 | 🟢 | 人工评审 | ⬜ |
| 49 | 9 个 Processor 重复 | 🟢 | 人工评审 | ⬜ |
| 54 | Qodana 样式问题 | 🟢 | Qodana | ⬜ |
| 79 | 设置页缺智能体名 | 🟢 | 插件UI优化 | ⬜ |
| 80 | 下拉缺角色描述 | 🟢 | 插件UI优化 | ⬜ |
| 82 | URL 框缺 placeholder | 🟢 | 插件UI优化 | ⬜ |
| 106 | ThreadLocal API Key 泄漏 | 🟢 | 会话日志分析 | ⬜ |
| 107 | ChatSession toString 不完整 | 🟢 | 会话日志分析 | ⬜ |
| 108 | ConnectionManager 同session多连接 | 🟢 | 会话日志分析 | ⬜ |
| 112 | answer_sync 覆盖不匹配 | 🟢 | 会话日志分析 | ⬜ |
| 113 | toolCallId Future 累积 | 🟢 | 会话日志分析 | ⬜ |
| 116 | LSP4J pusher 通知 WebUI session 错配 | 🟢 | 会话交叉分析 | ⬜ |
| 117 | 三端日志格式不统一 | 🟢 | 会话交叉分析 | ⬜ |
| 118 | IDE [Clawith] 前缀不一致 | 🟢 | 会话交叉分析 | ⬜ |
| 75 | ChatSessionUtil 全局Map | 💡 | 流畅度分析 | ⬜ 低风险 |
| 94 | UI 搜索卡片闪烁 | 💡 | 流畅度分析 | ⬜ |
| 109 | IDE 插件无 session 恢复 | 💡 | 会话日志分析 | ⬜ |
| 110 | chat/think 新行绕过节流（3441条/请求） | 🟠 | `jsonrpc_router.py:1791` `"\n" in text` | ⬜ 改为时间节流 200ms + 清除 thinking_chunks |
| 111 | ToolPanel 注册竞态每秒等 111 次 | 🟡 | `ChatToolEventProcessor.java:129-141` | ⬜ 轮询 1s→100ms，warn→debug |

> **本回合新增（5 项）**：#114 LSP4J create_task 无界增长 🟡 | #115 LSP4J 双重事务嵌套 🟡 | #116 LSP4J pusher 通知 WebUI session 错配 🟢 | #117 三端日志格式不统一 🟢 | #118 IDE [Clawith] 前缀不一致 🟢
>
> **累计已修复（92 项）**：含 #55/#56/#57 IDE 上下文链路 ✅、#62 前后端模型对齐 ✅、#74 IDE URL session_id ✅、#76 IDE 日志关联 ✅、#86 chat/finish sleep ✅、#95 Terminal API 2025.1 ✅、#79-#82 UI 优化 ✅、#87-#88/#90 流畅度瓶颈 ✅ 及早期修复 62 项；本轮新增 5 项：核查确认后续入

# Clawith 后端 + IDEA 插件 全量代码评审 — 问题清单

> 评审时间：2026-05-12 | 更新：2026-05-13（第 5 轮，移除已修复条目）
> 评审范围：Clawith 后端 `backend/` (365 个 Python 文件) + IDEA 插件 `demo-new/` (1361 个 Kotlin/Java 文件)

**共 15 个待处理问题**：🔴1　🟠0　🟡7　🟢7

---

## 🔴 严重（1 个）

### #1 — 智能体无法感知本地项目上下文

**原编号**: L3 | **位置**: `jsonrpc_router.py:129-141` + `_build_ide_environment_prompt()`
**状态**: 🔄 后端上下文注入已完整，需端到端验证

**三层断裂**：
1. 上下文注入链路 — `_build_ide_environment_prompt()` 已解析 `chatContext`（activeFilePath, selectedText 等）+ `sessionType` + `mode` + 代码地图注入
2. 工具可用性感知 — 系统提示词未说明"已连接本地IDE"，无首次连接环境注入消息
3. 工具调用反馈闭环 — UI 层看不到过程，LLM 层可能不知道如何利用返回结果

<details>
<summary>LSP4J 未适配功能 & 修复规划</summary>

**未适配功能**：

| 功能 | 协议方法 | 优先级 |
|------|---------|--------|
| 内联编辑操作 | `textDocument/nextEditAction` | P1 |
| 代码变更应用完成 | `chat/codeChange/apply/finish` | P1 |
| 快照同步 | `snapshot/syncAll` | P2 |
| 工作空间文件同步 | `workingSpaceFile/sync` | P2 |
| 聊天删除通知 | `chat/delete` | P2 |
| 全局配置变更 | `config/changeGlobal` | P2 |
| 模型列表刷新 | `config/refreshModels` | P2 |
| 代码引用查询 | `textDocument/queryReference` | P3 |
| PSI继承分析 | `psi/inherits` | P3 |
| PSI变量分析 | `psi/listVariables` | P3 |

**修复阶段**：

| 阶段 | 内容 | 状态 |
|------|------|------|
| P0 | 全局 Map 初始化、tool/call/sync 通知、requestId 传递 | ✅ 已完成 |
| P1 | 增强上下文提取、增强工具可用性系统提示 | ⬜ 待推进 |
| 体验优化 | Web 端工具调用可视化、Diff 预览、高风险工具确认 | ⬜ 待规划 |

**关键代码**：

| 侧 | 文件 | 要点 |
|----|------|------|
| 插件 | `LanguageClientImpl.java` | LSP 客户端，全局 Map 初始化 + 工具路由 |
| 插件 | `ChatThinkingProcessor.java` | 思考过程处理器 |
| 插件 | `ChatToolEventProcessor.java` | 工具调用事件核心处理器 |
| 后端 | `jsonrpc_router.py` | `_send_chat_think`、`invoke_tool_on_ide`、上下文注入 |
| 后端 | `tool_hooks.py` | LSP4J 工具定义与钩子 |
| 后端 | `context.py` | 上下文管理 |

</details>

---

## 🟡 中（8 个）

### #11 — 184 处 `except Exception:`（51 个文件），约 97 处静默吞异常

**原编号**: #8 | **位置**: `backend/app/api/agents.py:884-965`、`websocket.py`、`feishu.py`、`dingtalk.py`、`discord_bot.py` 等
**状态**: 🔄 部分修复（agents.py 8处已加日志，其余 89 处待评估）

```python
try: await agent_manager.remove_container(agent)
except Exception: pass
```

> **定性**: `agent_tools.py` 的 57 处是工具加载兜底（合理保留），其余静默吞异常应至少加 `logger.warning(exc_info=True)`。新增发现 `websocket.py` 5 处、`feishu.py`/`dingtalk.py`/`discord_bot.py` 多处静默吞异常。

---

### #12 — 95% 重复的 diff 算法代码 ✅

**原编号**: #11 | **位置**: `inlinediff/` (Kotlin, 9 文件) vs `ineditordiff/` (Java, 27 文件)
**状态**: ✅ 已修复 — 删除 14 个死代码文件（~573 行），详见 `diff-代码重复分析方案.md`

调研发现 `inlinediff/` 9 个文件无任何外部调用方，为孤立死代码；`ineditordiff/` 内 5 个文件（`PrefixDiffingAlgorithm` 等）仅被测试 main 调用，生产代码实际使用 `JGitDiffAlgorithmAdapter`（包装 JGit HistogramDiff）。两个自定义 DP 算法均未被生产使用，直接删除无需合并。

---

### #13 — OKR 模块绕过依赖注入

**原编号**: #13 | **位置**: `backend/app/api/okr.py` ~20 个端点
**状态**: 🔄 评估完成 — 需专项会话重构（20+ 处 `async_session()` + 多处嵌套 session db2/db3 需隔离评估）

OKR 路由直接创建 `async with async_session() as db:`，而项目其他模块统一 `Depends(get_db)`。导致风格不一致、嵌套 session 问题、测试困难。

**建议**: 统一改为 `db: AsyncSession = Depends(get_db)`，嵌套 session 需评估事务隔离需求后逐处处理。

---

### #21 — LSP4J 主版本升级（0.12 → 1.0.0）需端到端验证

**原编号**: #51 | **位置**: `demo-new/gradle/libs.versions.toml`

lsp4j 主版本跳跃 + `javax → jakarta` 迁移。编译通过但运行时需验证：ping、chat/answer、chat/think、tool/invoke、tool/call/sync。

---

### #23 — 390 个 `catch(Exception)` 宽泛捕获（插件侧）

**新发现**: N9 | **位置**: 插件全局
**状态**: 🔄 会话管线全部修复（ChatAnswerProcessor, ChatToolEventProcessor, ChatSessionUtil, CosyHeartbeatRunner 等 14 处已修复）

插件中 390 处 `catch(Exception)`（部分来自代码生成工具），可能吞掉 `NullPointerException` 等编程错误，掩盖真实 bug。

**建议**: 分优先级治理——Chat/Core/LSP 模块优先改为具体异常类型，tool/model 模块次之。

---

### #24 — LanguageClientImpl 1322 行过长（30+ @Override 方法）

**新发现**: N10 | **位置**: `demo-new/.../core/lsp/LanguageClientImpl.java`
**状态**: ⬜ 待重构

单文件 1322 行、30+ 个 `@Override` 方法，违反"文件聚焦 <800 行"原则。

**建议**: `buildProject` 功能 → Delegate 类、`Completion` 功能 → 独立 Handler、工具结果同步 → 独立模块。分 3 次提交拆分。

---

### #29 — Kotlin 仅占 ~5%（68/1361 文件），大量 POJO/Enum 可迁移

**新发现**: N15 | **位置**: 插件全局
**状态**: ⬜ 待规划

项目要求优先使用 Kotlin，但当前仅 ~5% 是 Kotlin 文件。大量 POJO（Params/Result 类）、Enum 可用 Kotlin data class/enum class 精简 30-50% 代码量。

**建议**: 分批迁移——新文件强制 Kotlin、POJO/Enum 逐步转换、工具 Handler 保留 Java（EDT 调试便利）。

---

### #31 — agent_tools.py 13609 行单体文件

**新发现**: N17 | **位置**: `backend/app/services/agent_tools.py`
**状态**: ⬜ 待评估

远超 800 行上限。文件包含 100+ 个工具函数，按功能域拆分（文件操作、搜索、Agent 通信、终端）可大幅提升可维护性。

**建议**: 评估拆分为 `tools/file_ops.py`、`tools/search.py`、`tools/agent_comms.py`、`tools/terminal.py`，通过注册机制加载。

---

## 🟢 低（7 个）

### 后端（3 个）

| # | 问题 | 原编号 | 位置 | 建议 |
|----|------|--------|------|------|
| 34 | `list_tasks()` 12 个参数 | #21 | `api/tasks.py` | 封装为 Pydantic Model 会改变 OpenAPI Schema，暂缓 |
| 36 | 无错误追踪集成 | #25 | 后端整体 | 可选 Sentry SDK |
| 37 | Docker SYS_ADMIN + docker.sock | #27 | `docker-compose.yml` | 非沙箱场景移除权限，考虑 rootless |

### IDEA 插件（4 个）

| # | 问题 | 原编号 | 位置 | 建议 |
|----|------|--------|------|------|
| 44 | 76 个工具类分散 | #22 | `util/` 包 | 按功能合并（大型重构） |
| 45 | `model/model` 路径嵌套 | #26 | `core/lsp/model/model/` | 重构为 `core/lsp/model/`（大型重构） |
| 47 | flexmark/swingx 未维护 | #45 | `libs.versions.toml` | 标记为传递依赖，跟踪替代方案 |
| 49 | 9 个 ChatXxxProcessor 重复模式 | #69 | `chat/processor/` | 提取 `AbstractChatProcessor` 基类 |

---

## 汇总

| # | 问题 | 严重程度 | 状态 |
|---|------|----------|------|
| 1 | 智能体看不到本地项目 | 🔴 | 🔄 需端到端验证 |
| 11 | 97处静默异常 | 🟡 | 🔄 部分修复 |
| 12 | diff代码重复 | 🟡 | ✅ 已修复 |
| 13 | OKR绕过DI | 🟡 | 🔄 需专项会话 |
| 21 | LSP4J 1.0 端到端验证 | 🟡 | ⬜ |
| 23 | 390 个 catch(Exception) 宽泛（插件） | 🟡 | 🔄 会话管线全修复 |
| 24 | LanguageClientImpl 1322 行过长 | 🟡 | ⬜ |
| 29 | Kotlin 仅占 5% | 🟡 | ⬜ |
| 31 | agent_tools.py 13609 行单体文件 | 🟡 | ⬜ |
| 34 | 参数过多 | 🟢 | 🔄 暂缓 |
| 36 | 无错误追踪 | 🟢 | ⬜ |
| 37 | Docker权限过大 | 🟢 | ⬜ |
| 44 | 工具类分散 | 🟢 | ⬜ |
| 45 | model/model嵌套 | 🟢 | ⬜ |
| 47 | flexmark/swingx 未维护 | 🟢 | ⬜ |
| 49 | 9 个 Processor 重复模式 | 🟢 | ⬜ |

> **已修复并剔除（53 个）**：原 #2, #3, #4, #7, #10, #12, #14, #15, #17, #18, #26, #32, #33, #42, #51, #53, #57-#68, L1, L2, L4, L5 + #46（SLF4J 桥接）、#56（tomcat 版本孤岛误报）、#57（libs.versions.toml 注释依赖）、#22（.zshrc 冲突已消失）、#27（CORS 已正确配置）、#16（print→logger）、#28（UserLogin max_length）、#35（结构化 JSON 日志）、#38（execute_code sync 警告优化）、#39（duckduckgo_search 网络超时已确认一次性波动）、#40（websocket.py 冗余 import）、#41（alembic mergepoint 延后处理）、#43（合并至 #33）

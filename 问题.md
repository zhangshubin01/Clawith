# Clawith 后端 + IDEA 插件 全量代码评审 — 问题清单

> 评审时间：2026-05-12
> 评审范围：
> - Clawith 后端: `backend/` (365 个 Python 文件)
> - IDEA 插件: `demo-new/` (1361 个 Kotlin/Java 文件)

---

## 🔴 严重（必须修复）

### #1 API Key 通过 URL Query String 传输

**位置**: `demo-new/.../ClawithSettingsState.java:buildClawithWebSocketUrl()`
**严重程度**: 🔴 严重

**描述**: API Key 经 `URLEncoder.encode` 放入 WSS URL 查询参数中传输（`wss://host/api/.../ws?agent_id=X&token=Y`）。URL 虽然加密传输，但：
- URL 出现在服务器日志、代理日志中
- WebSocket 库可能将 URL 记录到内存堆栈
- 与其他认证方式（JWT Header、X-Api-Key Header）不一致

**建议**: 改为在 WebSocket 握手时通过 HTTP Header（如 `Authorization: Bearer cw-xxx`）传递，或使用 WebSocket 子协议协商认证。

---


### #3 级联删除使用原始 SQL 无事务保护

**位置**: `backend/app/api/agents.py:862-968`
**严重程度**: 🔴 严重

**描述**: `delete_agent()` 使用 ~8 条硬编码原始 SQL DELETE 语句（涉及 15 张关联表）清理关联数据：
```python
cleanup_tables = [
    "agent_activity_logs", "audit_logs", "approval_requests",
    "chat_messages", "chat_sessions", "agent_schedules",
    "agent_triggers", "channel_configs", "agent_permissions",
    "agent_tools", "agent_relationships", "gateway_messages",
    "published_pages", "notifications", "daily_token_usage",
]
for table in cleanup_tables:
    await db.execute(text(f"DELETE FROM {table} WHERE agent_id = :aid"), ...)
```
**风险**:
- 每次新增关联表，如果忘记加到这个列表，删除时会留下孤立数据
- 每条 SQL 使用独立的 `begin_nested()` savepoint，但整体无原子性
- 每条都用 `except Exception: pass` 吞异常

**建议**: 在 SQLAlchemy Model 中配置 `relationship(..., cascade="all, delete-orphan")`，一次 `db.delete(agent)` 自动级联清理。

---

### #4 AES 密钥派生无盐值

**位置**: `backend/app/core/security.py:46-48`
**严重程度**: 🔴 严重

**描述**:
```python
aes_key = hashlib.sha256(key_bytes).digest()
```
直接用 SHA-256 单次哈希派生 AES-256 密钥，无 salt、无迭代。当多个密文使用相同密钥加密时，安全性降低。

**建议**: 使用 PBKDF2 或 HKDF：
```python
from Crypto.Protocol.KDF import PBKDF2
aes_key = PBKDF2(key, b"", dkLen=32, count=100000)
```

---


### #6 email-hint 端点泄露用户存在性

**位置**: `backend/app/api/auth.py:561-597`
**严重程度**: 🔴 严重

**描述**: `/api/auth/email-hint?username=xxx` 端点返回部分混淆的邮箱地址（如 `a***b@c***d.com`）。任何已登录用户都可以枚举用户名：
- 确认账户是否存在 → `404` vs `200`
- 即使混淆了邮箱，攻击者仍能逐步缩小邮箱范围
- 前端忘记密码页需要此功能，但缺乏爆破防护

**建议**:
- 添加速率限制（同一 IP 每分钟最多 5 次）
- 无论用户是否存在，统一返回 200（防用户枚举）
- 考虑用验证码替代邮箱提示

---

---

## 🟡 中（应尽快修复）

### #8 184 处 `except Exception:`（51 个文件中），约 97 处静默吞异常

**统计**: `grep -r "except Exception:" backend/app/ --include="*.py" -c` → **184 处**，分布在 **51 个文件**中，其中约 97 处为静默 `pass`
**位置**: `backend/app/api/agents.py:884-965`, `backend/app/plugins/clawith_lsp4j/jsonrpc_router.py`, `backend/app/services/agent_tools.py`
**严重程度**: 🟡 中

**描述**: 大量 `except Exception: pass` 或 `except Exception:` 后跟空的 `pass` 块。

```python
# agents.py 删除方法中连续 8 处
try:
    await agent_manager.remove_container(agent)
except Exception:
    pass
try:
    archive_dir = await agent_manager.archive_agent_files(agent.id)
except Exception:
    pass
# ... 还有 6 处
```


> **定性说明**: `agent_tools.py` 的 57 处 `except Exception` 是工具加载时的合理兜底逻辑（单个工具加载失败不应阻止其他工具加载），与 `agents.py` 中的静默吞异常有本质区别：
> - **合理兜底**（`agent_tools.py` 57 处）：建议保留，可加 `logger.debug()` 辅助排查
> - **应修复**（`agents.py` 8 处等）：静默吞异常，至少加 `logger.warning(exc_info=True)`

**建议**: 每个异常处理至少加 `logger.warning(f"...", exc_info=True)`，便于线上问题排查。

---


### #11 95% 重复的 diff 算法代码

**位置**: `inlinediff/` (Kotlin) vs `ineditordiff/` (Java)
**严重程度**: 🟡 中

**描述**: 两个包实现几乎相同的 diff 算法，一个用 Kotlin、一个用 Java。
已在 `REFACTORING_ASSESSMENT_REPORT.md` 中记录。

**建议**: 统一为一个实现（推荐 Kotlin），另一个改为调用统一实现。

> 🔄 **评估**: `inlinediff/` (Kotlin, 9 个文件) + `ineditordiff/` (Java, 25 个文件)，涉及 34 个文件的重构。两个包虽然共享 `PrefixDiffingAlgorithm`、`Array2D`、`ISequence` 等算法核心，但渲染层（UI 组件、Inlay 管理）差异较大，直接合并风险高。建议作为专项重构任务单独处理。

---

### #13 OKR 模块绕过依赖注入

**位置**: `backend/app/api/okr.py` ~10 个端点
**严重程度**: 🟡 中

**描述**: OKR 路由模块中约 10 个端点直接创建自己的数据库会话：
```python
async with async_session() as db:
    # ...
```
而项目中其他 API 模块统一使用 FastAPI 依赖注入 `Depends(get_db)`。这导致：
- 与全局 DI 风格不一致
- `update_okr_settings()` 中出现嵌套 session 问题（外层 DI session 读取，内层 `async_session()` 写入）
- 无法在测试中统一 mock 数据库会话

**建议**: 统一改为 `db: AsyncSession = Depends(get_db)`。

---

### #14 `_compute_current_period` 和 `_compute_period_for_date` 重复

**位置**: `backend/app/api/okr.py:238-269` 和 `:272-295`
**严重程度**: 🟡 中

**描述**: 两个函数逻辑 95% 相同（monthly/custom/quarterly 分支完全一致），差别仅在参数：
- `_compute_current_period(frequency, length_days)` — 用今天的日期
- `_compute_period_for_date(frequency, length_days, target)` — 用指定日期

**建议**: 合并为一个函数：
```python
def _compute_period(frequency: str, length_days: int | None, target: date | None = None) -> tuple[date, date]:
    target = target or date.today()
    # ... 统一逻辑
```

---

### #15 身份验证逻辑重复

**位置**: `backend/app/core/security.py:get_current_user()` vs `verify_api_key_or_token()`
**严重程度**: 🟡 中

**描述**: 两个函数中 API Key 验证逻辑（SHA-256 哈希 → 数据库查询 → hmac.compare_digest → is_active 检查）完全一致，JWT 验证也几乎相同。代码重复 >80%。

**建议**: 提取公共函数：
```python
async def _authenticate_by_api_key(db, api_key: str) -> User: ...
async def _authenticate_by_jwt(db, token: str) -> User: ...
```

---


### #17 使用 `print()` 代替 logger

**位置**: `backend/app/main.py:201,203,205`, `backend/app/api/upload.py:52-76`
**严重程度**: 🟡 中

**描述**:
```python
# main.py startup 迁移
print(f"[startup] ✅ Migrated enterprise_info → enterprise_info_{_tenant.id}", flush=True)

# upload.py PDF 解析
result = subprocess.run(["python3", "-c", f"""...print(text[:8000])..."""])
```

**建议**:
- `main.py` 的 `print()` 改为 `logger.info()`
- `upload.py` 的 subprocess 内 `print()` 是必要的（捕获 stdout），但外层应加 `logger.debug()` 记录输出

---

### #18 URL 路径不一致

**位置**: `backend/app/main.py:400-404`
**严重程度**: 🟡 中

**描述**: 部分路由缺少 API 前缀：
```python
app.include_router(triggers_router)       # 无 prefix
app.include_router(chat_sessions_router)  # 无 prefix
app.include_router(plaza_router)          # 无 prefix
```
带 prefix 的：
```python
app.include_router(agents_router, prefix=settings.API_PREFIX)  # 有 prefix
```

**建议**: 统一使用 `prefix=settings.API_PREFIX`，除非路由自己管理前缀。

---

### #19 异常处理丢失根因链

**位置**: `backend/app/core/security.py:98`
**严重程度**: 🟡 中

**描述**:
```python
except Exception as e:
    raise ValueError(f"Decryption failed: {e}") from e
```
catch Exception 范围太宽，AES 错误、Unicode 错误、base64 错误全部被转换为 `ValueError`，丢失了原始异常类型信息。

**建议**: 区分具体异常类型，或至少保留原始异常类：
```python
except (ValueError, KeyError) as e:
    raise ValueError(f"Decryption failed: invalid data format") from e
except Exception as e:
    logger.error(f"Unexpected decryption error: {e}", exc_info=True)
    raise ValueError(f"Decryption failed unexpectedly") from e
```

---

## 🟢 低（可后续改进）

### #20 大量模块级 docstring 缺失

**位置**: `backend/app/config.py`（仅 1 行），`backend/app/api/tools.py`，多个 services
**严重程度**: 🟢 低

**描述**: 多个模块缺少中文 docstring 说明模块职责。

**建议**: 每个模块添加简要的中文 docstring。

---

### #21 函数参数过多

**位置**: `backend/app/api/auth.py:list_tasks()` — 13 个参数
**严重程度**: 🟢 低

**描述**: 函数参数过多，可读性差，调用容易出错。

**建议**: 封装为 Pydantic Model 或 dataclass。

---

### #22 76 个工具类分散

**位置**: IDEA 插件 `util/` 包
**严重程度**: 🟢 低

**描述**: `StringUtils` 和 `StringCheckUtil` 存在功能重叠，76 个工具类分散不利于维护。

**建议**: 按功能合并相关工具类。

---

### #24 无结构化 JSON 日志

**位置**: `backend/app/core/logging_config.py`
**严重程度**: 🟢 低

**描述**: 日志格式为纯文本，不利于 ELK/Loki 等日志聚合系统的解析。

**建议**: 添加可选的 JSON 格式输出，通过 `LOG_FORMAT=json` 环境变量切换。

---

### #25 无错误追踪集成

**位置**: 后端整体
**严重程度**: 🟢 低

**描述**: 未集成 Sentry 或其他错误追踪系统。生产环境的异常无法实时追踪和归类。

**建议**: 添加 Sentry SDK 集成（可选，通过环境变量控制开关）。

---

### #26 model/model 路径嵌套

**位置**: IDEA 插件 `core/lsp/model/model/`
**严重程度**: 🟢 低

**描述**: 目录路径中出现 `model/model` 嵌套，命名冗余。

**建议**: 重构为 `core/lsp/model/` 或 `core/lsp/types/`。

---

### #27 Docker SYS_ADMIN + docker.sock 挂载

**位置**: `docker-compose.yml`
**严重程度**: 🟢 低

**描述**: 后端容器需要 `SYS_ADMIN` capability 和 `docker.sock` 挂载用于 Agent 沙箱（Docker-in-Docker）。这是必要的但增大了攻击面。

**建议**: 
- 对非沙箱部署场景，移除这些权限
- 考虑 rootless Docker 或 subprocess 沙箱模式

---

### #28 IDEA 插件无 SLF4J 桥接

**位置**: IDEA 插件整体
**严重程度**: 🟢 低

**描述**: SLF4J 仅通过 jgit 传递依赖引入，插件未配置 logback/桥接。

**建议**: 添加 `slf4j-intellij` 桥接或显式排除 jgit 带来的 SLF4J 依赖。

---

---

## LSP4J 集成三大问题深度调研

> 调研时间：2026-04-26 | 状态：✅ 深度评审完成，根因验证完毕
> 涉及模块：灵码插件 + Clawith 后端 clawith_lsp4j 插件

### LSP4J 问题总览

| 编号 | 问题描述 | 严重程度 | 根因类型 | 验证状态 |
|------|---------|---------|---------|---------|
| L1 | 思考过程、工具调用进度在聊天面板完全不显示 | 🔴 P0 | 全局状态未初始化 + 协议不匹配 | ✅ 插件端已修复，后端已验证 |
| L2 | AI生成代码变更后，无Diff对比窗口和接受/拒绝按钮 | 🟠 P1 | 实现缺失 + 协议未打通 | ⬜ 待修复 |
| L3 | 智能体无法感知本地项目上下文，一直回复"看不到文件" | 🔴 P0 | 上下文链路断裂 + 工具UI通知缺失 | 🔄 后端上下文已完整，需端到端验证 |

### L1：插件不显示思考过程 + 工具调用过程

> 🔬 **2026-05-12 日志验证**：分析 IDE 插件日志 (`idea.log`) 和后端日志 (`backend.log`) 确认

**日志验证结果**：

| 消息类型 | 后端发送 | 插件接收 | 状态 |
|---------|---------|---------|------|
| `tool/call/sync` | ✅ 正常发送（PENDING→RUNNING→FINISHED） | ✅ 正常接收派发 | ✅ 已修复 |
| **`chat/think`** | ✅ 后端有 `[LSP4J →]` 日志 | **❌ 插件侧 0 条日志** | ❌ 静默丢弃 |
| **`chat/answer`** | ✅ 后端有流式输出 | **❌ 插件侧 0 条日志** | ❌ 静默丢弃 |
| **`chat/process_step_callback`** | ✅ 后端有发送 | **❌ 插件侧 0 条日志** | ❌ 静默丢弃 |

IDE 插件 `[Clawith]` 日志分类统计（共 504 条）：
```
[Clawith][WebSocket]     96  ← 连接层正常
[Clawith][ToolInvoke]    64  ← 工具调用正常
[Clawith][ToolPanel]     47  ← 工具面板 UI 正常
[Clawith][InputGate]     28  ← 输入发送正常
[Clawith][BaseChatPanel] 12  ← 聊天面板极少
[Clawith][ChatThinking]   0  ← ❌ 思考过程完全不显示！
[Clawith][ChatAnswer]     0  ← ❌ 答案推送无日志！
```

**会话不流畅的根因**：
1. 思考过程不显示 → 用户不知道 AI 在"想什么"，等待时无反馈
2. 答案流式输出可能断裂 → 用 `_send_notification` 格式与 `@JsonRequest` 不匹配
3. 工具间间隙无 UI 反馈 → 两次工具调用间 3-8 秒完全空白，用户只能干等
4. requestId 不匹配 → 4 个全局 Map 未初始化，处理器静默丢弃消息

**根因 A：4 个全局 UserData Map 未初始化**

灵码插件通过 4 个全局 Map 关联 `requestId` 到各种状态（`CosyKey.java`）：
- `REQUEST_TO_PROJECT` — requestId → Project 对象映射
- `REQUEST_TO_SESSION_TYPE` — requestId → sessionType 映射
- `REQUEST_TO_ANSWER_LIST` — requestId → 回答列表映射
- `REQUEST_ANSWERING` — requestId → 时间戳

原生模式在 `CosyServiceImpl.java:161-207` 初始化这些 Map。LSP4J 模式绕过了 `CosyServiceImpl`，**Map 从未初始化**。

所有消息处理器强依赖这些 Map，找不到时静默丢弃：
- `ChatThinkingProcessor.java:64-69` — 找不到直接 `return false`
- `ChatProcessStepCallbackParamsProcessor.java:57` — 找不到直接 `return false`
- `ChatAnswerProcessor.java:55-60` — 但**有 Clawith 特殊适配**可以绕过

**根因 B：`tool/call/sync` 通知完全缺失**

工具调用 UI 卡片渲染依赖 `tool/call/sync` 通知（非 `tool/invoke`）。`ChatToolEventProcessor.java` 通过 `toolCallSync()` 方法接收事件。`ToolCallSyncResult.projectPath` 字段是必需的，为空则事件被静默丢弃。

**根因 C：工具调用超时关联问题**

`invoke_tool_on_ide()` 生成新 UUID 作为 requestId，但 IDE 端也通过 `REQUEST_TO_PROJECT[requestId]` 查找 Project。requestId 不匹配导致查找失败。不过 `toolInvoke` 有兜底逻辑（找不到时用 `ProjectUtils.getActiveProject()`），思考过程和步骤回调则没有兜底。

**消息类型不匹配**：

| 消息方法 | 插件注解 | Clawith发送方式 | 匹配状态 |
|---------|---------|----------------|---------|
| `chat/think` | `@JsonRequest` | `_send_notification` | ❌ 不匹配 |
| `chat/process_step_callback` | `@JsonRequest` | `_send_notification` | ❌ 不匹配 |
| `tool/call/sync` | `@JsonRequest` | 未发送 | ❌ 缺失 |
| `chat/answer` | `@JsonRequest` | `_send_notification` | ❌ 但有特殊适配 |

> ✅ **2026-05-12 IDEA 插件端已修复（根因 A + C）**: `LanguageClientImpl.java` 新增 `ensureLsp4jRequestRegistered()` 方法，在 `chatThinking()`/`answer()`/`processStepCallback()`/`finish()`/`toolInvoke()` 入口处补全全局 Map 初始化。后端经验证已统一使用 `_send_client_request`（带 JSON-RPC id）匹配 `@JsonRequest`。

### L2：插件没有显示代码比对 / 同意拒绝弹窗

**双缺失**：
1. 插件侧 `LanguageClientImpl.java` 未覆盖 `applyEdit` 方法（默认抛出 `UnsupportedOperationException`）
2. Clawith 后端 `jsonrpc_router.py` 完全没有 `workspace/applyEdit` 相关代码

**推荐方案**：采用 `RewriteCodeAction + tool/call/sync` 方式，与现有工具调用UI框架统一，支持审批流程。

### L3：智能体一直回复"看不到插件本地项目"

**三层断裂**：
1. 上下文注入链路不完整 — `jsonrpc_router.py:129-141` 仅提取了 `params.extra.context`，缺少 `chatContext`、`sessionType`、`mode` 等字段 → 🔄 经验证 `_build_ide_environment_prompt()` 已完整解析 `chatContext`（activeFilePath, selectedText 等）+ `sessionType` + `mode` + 代码地图注入
2. 工具可用性感知不足 — 系统提示词未说明"已连接本地IDE"，无首次连接环境注入消息
3. 工具调用反馈闭环缺失 — UI 层看不到过程，LLM 层可能不知道如何利用返回结果 → 🔄 L1 修复后应改善

### L4：会话不流畅 — chat/think 和 chat/answer 插件侧 0 日志，用户无反馈

**位置**: IDE 插件 + `backend/app/plugins/clawith_lsp4j/jsonrpc_router.py`
**严重程度**: 🔴 P0

**描述**: 工具间等待期完全空白，两次工具调用之间 3-8 秒没有任何 UI 反馈。具体表现：
- `chat/think` 思考过程 — 插件侧 0 条日志，后端有发送但插件静默丢弃
- `chat/answer` 答案流式输出 — 插件侧 0 条日志，`_send_notification` 格式与 `@JsonRequest` 不匹配
- `chat/process_step_callback` 步骤回调 — 插件侧 0 条日志，无兜底逻辑

IDE 插件 `[Clawith]` 日志分类统计（共 504 条）：
```
[Clawith][WebSocket]     96  ← 连接层正常
[Clawith][ToolInvoke]    64  ← 工具调用正常
[Clawith][ToolPanel]     47  ← 工具面板 UI 正常
[Clawith][InputGate]     28  ← 输入发送正常
[Clawith][BaseChatPanel] 12  ← 聊天面板极少
[Clawith][ChatThinking]   0  ← ❌ 思考过程完全不显示！
[Clawith][ChatAnswer]     0  ← ❌ 答案推送无日志！
```

**根因**: 与 L1 共享根因（4 个全局 Map 未初始化 + 消息类型不匹配），但聚焦于用户体验层面——用户看不到任何中间反馈，只能干等。

**建议**: 修复 L1 的 4 个全局 Map 初始化和消息类型匹配后，此问题应同步解决。

> ✅ **2026-05-12 已修复**: 与 L1 同步修复，4 个全局 Map 初始化完成后 L4 应同步解决。

### 未适配功能清单

| 功能 | 协议方法 | 优先级 | 说明 |
|------|---------|--------|------|
| 代码引用查询 | `textDocument/queryReference` | P3 | 查找类/方法的引用位置 |
| PSI继承分析 | `psi/inherits` | P3 | 分析类继承关系 |
| PSI变量分析 | `psi/listVariables` | P3 | 列出文件中所有变量 |
| 快照同步 | `snapshot/syncAll` | P2 | 代码快照版本管理 |
| 工作空间文件同步 | `workingSpaceFile/sync` | P2 | 文件变更实时同步 |
| 聊天删除通知 | `chat/delete` | P2 | 会话删除同步 |
| 全局配置变更 | `config/changeGlobal` | P2 | 配置同步 |
| 模型列表刷新 | `config/refreshModels` | P2 | 模型选择同步 |
| 内联编辑操作 | `textDocument/nextEditAction` | P1 | 代码快速编辑动作 |
| 代码变更应用完成 | `chat/codeChange/apply/finish` | P1 | 变更应用结果通知 |

### LSP4J 修复优先级

**工具调用时序分析**（来自 `idea.log` 实测）：

| # | PENDING 时间 | FINISHED 时间 | 耗时 | 命令 |
|---|-------------|--------------|------|------|
| 1 | 18:29:35.503 | 18:29:36.605 | 1.1s | 简单命令 |
| 2 | 18:29:47.097 | 18:29:48.231 | 1.1s | 简单命令 |
| 3 | 18:30:05.972 | 18:30:07.057 | 1.1s | 简单命令 |
| 4 | 18:30:22.076 | 18:30:23.197 | 1.1s | 简单命令 |
| 5 | 18:30:32.277 | 18:30:33.399 | 1.1s | 简单命令 |
| 6 | 18:30:41.483 | 18:30:42.619 | 1.1s | 简单命令 |
| 7 | 18:30:46.925 | 18:30:55.538 | **8.6s** | gradle 构建 |
| 8 | 18:30:58.957 | 18:31:04.016 | 5.1s | gradle 构建 |
| 9 | 18:31:08.115 | 18:31:13.115 | 5.0s | gradle 构建 |
| 10 | 18:31:15.841 | 18:31:21.493 | 5.7s | gradle 构建 |

> ⚠️ 工具间等待期**完全空白**：两次工具调用之间 3-8 秒没有任何 UI 反馈（无思考过程、无进度提示、无答案流式输出），用户只能干等。

**第一阶段 P0（1-2 天）**：
- ~~修复 4 个全局 Map 初始化（插件侧 `LanguageClientImpl`）~~ ✅ 已完成
- ~~实现 `tool/call/sync` 通知（后端 `jsonrpc_router.py`，必须包含 `projectPath`）~~ ✅ 已验证存在
- ~~修复 `tool/invoke` 的 requestId 传递~~ ✅ 随全局 Map 修复解决
- 增加全链路诊断日志

**第二阶段 P1（2-3 天）**：
- 实现代码变更 Diff 预览（通过 `tool/call/sync` 发送 `RewriteCodeAction`）
- 增强上下文提取逻辑（`chatContext`、`sessionType`、`mode` 等）
- 增强工具可用性系统提示

**第三阶段 体验优化（1-2 天）**：
- 统一消息类型（Notification → Request）
- Web 端工具调用过程可视化
- Web 端 Diff 预览支持
- 高风险工具用户确认机制

### LSP4J 关键代码位置

**插件侧** (`demo-new/src/main/java/com/alibabacloud/intellij/cosy/`)：

| 文件 | 关键内容 |
|------|---------|
| `CosyServiceImpl.java:153-210` | chatAsk 方法，设置全局 Map 的参考实现 |
| `ChatThinkingProcessor.java:57-145` | 思考过程处理器，**无兜底** |
| `ChatProcessStepCallbackParamsProcessor.java:50-129` | 步骤回调处理器，**无兜底** |
| `ChatAnswerProcessor.java:55-86` | 答案处理器，**有 Clawith 特殊适配** |
| `ChatToolEventProcessor.java` | 工具调用事件核心处理器 |
| `LanguageClientImpl.java:1449-1488` | toolInvoke，**2 层查找，有兜底** |
| `LanguageClientImpl.java:1500-1522` | toolCallSync，强依赖 `projectPath` |
| `LanguageClient.java:633-636` | toolCallSync 接口定义 |
| `LanguageClient.java:91-94` | applyEdit 接口定义 |

**后端侧** (`backend/app/plugins/clawith_lsp4j/`)：

| 文件 | 关键内容 |
|------|---------|
| `jsonrpc_router.py:802-819` | `_send_chat_think` 方法 |
| `jsonrpc_router.py:844-858` | `_send_process_step_callback` 方法 |
| `jsonrpc_router.py:709-765` | `invoke_tool_on_ide` 方法（需修改 requestId） |
| `jsonrpc_router.py:129-141` | 上下文注入提取逻辑（需增强） |
| `tool_hooks.py` | LSP4J 工具定义与钩子 |
| `context.py` | 上下文管理 |

> 以上 LSP4J 5 个问题（L1-L5）与前面通用问题合计 **共 26 个问题**（本轮删除 5 个已修复项 #7, #10, #12, #23, #29）。

---

### L5：think:: 围栏流式注入缺失

**位置**: `backend/app/plugins/clawith_lsp4j/jsonrpc_router.py` + `stream_buffer.py`
**严重程度**: 🟠 P1
**描述**: 灵码插件解析 `` ```think:: … `` Markdown 围栏块 → 渲染 `ThinkingComponent`（可折叠，带计时）。Clawith 后端 `chat/think` 通道已接通，但**缺少流内 `think::` 围栏注入逻辑**。工具间的模型说明文字混在普通 Markdown 正文中，不区分"分析规划"和"最终答案"。

灵码有两条独立的"深度思考"展示通道：

| 通道 | 协议 | 布局 | 用途 |
|------|------|------|------|
| `chat/think` | `ChatThinkingParams` JSON-RPC | 回答上方北向思考区 | 全局推理（如 DeepSeek reasoning_content） |
| `` ```think::`` `` | chat/answer 流内 Markdown 围栏 | 工具卡片同一垂直时间线 | 工具间阶段说明（可折叠，带计时） |

**方案**: 在流式输出中，当检测到"工具完成 → 下轮 LLM 开始"的状态切换时，自动注入 `` ```think:: `` 围栏包裹中间说明文字，实现与灵码原生 `ThinkingComponent` 一致的渲染效果。

---


---

## 🟢 补充建议

### M1 数据库迁移 mergepoint 过多

**位置**: `backend/alembic/versions/`
**严重程度**: 🟢 低

**描述**: `alembic history` 输出含 7 个 mergepoint，迁移图复杂。每次合并分支都产生新的 mergepoint，长期维护成本递增。

**建议**: 在下一个大版本发布前执行 `alembic squash`，将中间 mergepoint 压缩为线性迁移链。

---

## 🔴 第二轮审查新增问题（IDEA 插件代码质量）

> 审查时间：2026-05-12 | 来源：3 个并行 Agent 审查结果

### #30 LSP_EXECUTOR 线程池永不关闭

**位置**: `demo-new/.../core/lsp/LanguageClientImpl.java:163-168`
**严重程度**: 🔴 严重

**描述**:
```java
private static final java.util.concurrent.ExecutorService LSP_EXECUTOR =
    java.util.concurrent.Executors.newFixedThreadPool(4, r -> {
        Thread t = new Thread(r, "LSP-Notify");
        t.setDaemon(true);
        return t;
    });
```
- `static final` 线程池，无任何 `shutdown()` 调用点
- 所在类 `LanguageClientImpl` 未实现 `Disposable` 接口
- 每次插件加载/卸载周期会累积线程，最终泄漏

**建议**: `LanguageClientImpl` 实现 `Disposable`，在 `dispose()` 中调用 `LSP_EXECUTOR.shutdown()`。

---

### #31 心跳线程未命名且未设为守护线程（3 处）

**位置**: `demo-new/.../core/Cosy.java:427,473,510`
**严重程度**: 🔴 严重

**描述**:
```java
// 3 处相同模式
new Thread(runner).start();
```
- 线程未 `setName()`，线程 dump 中只能看到 `Thread-XX`，无法快速定位
- 线程未 `setDaemon(true)`，JVM 退出时不会自动终止，可能导致进程僵死

**建议**: 三处统一改为 `new Thread(runner, "Cosy-Heartbeat-xxx").start()` + `setDaemon(true)`。

---

## 🟡 第二轮审查新增问题（中等）

### #32 DatabaseChangeListener 链式调用无 NPE 保护

**位置**: `demo-new/.../listener/DatabaseChangeListener.java:51-60`
**严重程度**: 🟡 中

**描述**:
```java
Cosy.INSTANCE.getLanguageService(this.project).getServer()
    .getTextDocumentService().didSave(params);
```
链式调用中任何一环返回 `null` 都会抛出 `NullPointerException`。该代码在数据库变更回调中执行，异常会被吞没。

**建议**: 拆分为逐步 null check。

---

### #33 invokeLater 中的过期引用

**位置**: `demo-new/.../core/lsp/LanguageClientImpl.java:357-377`
**严重程度**: 🟡 中

**描述**: `ApplicationManager.getApplication().invokeLater(() -> { ... })` 中的 Runnable 捕获了外部可变变量，当 Runnable 延迟执行时变量可能已被修改。

**建议**: 在 lambda 外捕获 `final` 副本后再传入。

---

### #34 构造函数上的 @Contract(pure=true) 与可变状态矛盾

**位置**: `demo-new/.../core/Cosy.java:99-101`
**严重程度**: 🟡 中

**描述**:
```java
@Contract(pure = true)  // ← 声明无副作用，但下面初始化了 4 个可变字段
private Cosy() {
    this.readyMap = new ConcurrentHashMap<>();
    this.languageServiceMap = new ConcurrentHashMap<>();
    this.statQps = new SlideWindowStatQps();
    this.cosyStartRetryTimes = 0;
}
```
`pure=true` 表示方法无副作用，误导 IntelliJ 静态分析器。

**建议**: 移除 `@Contract(pure = true)` 注解。

---

### #35 DatabaseUtil 零日志（452 行，17 个方法）

**位置**: `demo-new/.../db/DatabaseUtil.java`
**严重程度**: 🟡 中

**描述**: 452 行数据库工具类，无 `LOG` 实例。关键方法 `getAllDataSources()`、`getDataSourceTables()`、`generateDDL()` 执行外部连接操作，失败时无日志可追踪。

**建议**: 添加 `LoggerFactory.getLogger(DatabaseUtil.class)`，在关键路径加日志。

---

### #36 ToolHandler 写操作生命周期无日志

**位置**: `demo-new/.../cosy/tool/ToolHandler.java:93-129`
**严重程度**: 🟡 中

**描述**: `runWriteActionWithTimeout` 执行 IntelliJ Platform 写操作，涉及文件修改，但无开始/完成/超时日志。超时 30 秒时无任何记录。

**建议**: 添加 `LOG.debug()` 记录 WriteCommandAction 开始/完成，超时加 `LOG.warn()`。

---

### #37 ToolInvokeProcessor default 分支静默失败

**位置**: `demo-new/.../cosy/tool/ToolInvokeProcessor.java:281-286`
**严重程度**: 🟡 中

**描述**:
```java
default:
    // Unknown tool, silently ignored
    break;
```
遇到未识别的工具类型静默忽略，无任何日志。后端新增工具时难以排查。

**建议**: 添加 `LOG.error("Unexpected tool type: {}", toolName)` 并抛出异常。

---

### #38 6 处使用 jsoup 内部 API

**位置**: `demo-new/` 多处（`org.jsoup.internal.StringUtil`）
**严重程度**: 🟡 中

**描述**: 引用了 jsoup 的内部 API `org.jsoup.internal.StringUtil`。jsoup 不保证内部类的跨版本兼容性。

**建议**: 替换为 `org.apache.commons.lang3.StringUtils.isBlank()`（已是项目传递依赖）。

---

## 🟢 第二轮审查新增问题（低优先级）

### #39 9 个核心文件缺少详细中文 Javadoc

**位置**: 
- `CosyWebSocketMessageHandler.java`
- `CosyWebSocketMessageConsumer.java`
- `CosyMessageJsonHandler.java`
- `CosyWebSocketLauncherBuilder.java`
- `CosyHeartbeatRunner.java`
- `LanguageConnectClient.java`
- `DatabaseChangeListener.java`
- `ReadFileToolHandler.java`
- `SaveFileToolHandler.java`
**严重程度**: 🟢 低

**描述**: 上述 9 个文件缺中文注释，参考 `ToolHandler.java` 和 `CosyWebSocketConnectClient.java` 已有完整注释风格。

**建议**: 参考已有注释风格添加类级和方法级中文 Javadoc。

---

### #40 日志级别不当：WebSocket close 时 project null

**位置**: `demo-new/.../websocket/CosyWebSocketConnectClient.java:256`
**严重程度**: 🟢 低

**描述**: WebSocket close 回调中使用 `System.err.println` 输出，无时间戳、无上下文。

**建议**: 改为 `LOG.warn("WebSocket closed, project is null")`。

---

### #41 长 WebSocket 消息未截断

**位置**: `demo-new/.../lsp/CosyWebSocketMessageHandler.java:47`
**严重程度**: 🟢 低

**描述**: 接收到的 WebSocket 消息未经截断直接打印日志，包含文件内容的长消息可能导致单条日志数 MB。

**建议**: 截断到 500 字符。

---

### #42 心跳异常日志级别未区分

**位置**: `demo-new/.../core/CosyHeartbeatRunner.java:71-73`
**严重程度**: 🟢 低

**描述**: 心跳线程所有异常统一用 `LOG.error()`，`InterruptedException` 和 `SocketTimeoutException` 不应与未知异常同等对待。

**建议**: 区分 `InterruptedException`（warn + interrupt）、`SocketTimeoutException`（warn）、其他（error）。

---

### #43 Jakarta WebSocket 版本小幅落后

**位置**: `demo-new/gradle/libs.versions.toml`
**严重程度**: 🟢 低

**描述**: `jakarta-websocket-api` 和 `jakarta-websocket-client-api` 使用 2.1.1，最新稳定版 2.2.0。

**建议**: 升级到 2.2.0。

---

### #44 xtend-lib-macro 冗余显式声明

**位置**: `demo-new/gradle/libs.versions.toml`
**严重程度**: 🟢 低

**描述**: `xtend-lib-macro` 无源码引用（0 处），通过其他依赖传递引入，显式声明可移除。

**建议**: 从 `libs.versions.toml` 和 `build.gradle.kts` 中移除。

---

### #45 两个长期未维护的传递依赖

**位置**: `demo-new/gradle/libs.versions.toml`
**严重程度**: 🟢 低

**描述**:
- **flexmark 0.64.8**: 自 2023 年 5 月停止更新
- **swingx**: 自 2013 年冻结

均为 IntelliJ Platform 传递依赖，非插件直接使用。

**建议**: 确认未直接使用后，添加注释标记为"传递依赖，跟踪上游替代方案"。

---

### #46 L2 经确认为 L1 级联问题（非新问题）

**位置**: `问题.md` L2 条目
**严重程度**: 🟢 低（非新问题）

**描述**: 经深度审查确认：插件端已有完整的 Diff 基础设施（`RewriteCodeAction`、`ChatAskApplyDiffHandler`、`InlineDiffManager`），后端已通过 `tool/call/sync` 发送。Diff 弹窗不显示是因 L1 的 4 个全局 Map 未初始化导致 `toolCallSync` 静默丢弃。

**结论**: L1 修复后 L2 应自动解决。

---

## 核对状态

| # | 问题 | 严重程度 | 状态 |
|---|------|----------|------|
| 1 | API Key URL 传输 | 🔴 | ⬜ 待修复 |
| 3 | 级联删除原始SQL(8条) | 🔴 | ⬜ 待修复（后端） |
| 4 | AES 密钥派生 | 🔴 | ⬜ 待修复（后端） |
| 6 | email-hint 泄露 | 🔴 | ⬜ 待修复（后端） |
| 8 | 97处静默异常 | 🟡 | ⬜ 待修复（后端为主） |
| 11 | diff代码重复 | 🟡 | 🔄 评估为大型重构，需专项处理 |
| 13 | OKR绕过DI | 🟡 | ⬜ 待修复（后端） |
| 14 | compute_period重复 | 🟡 | ⬜ 待修复（后端） |
| 15 | 身份验证重复 | 🟡 | ⬜ 待修复（后端） |
| 16 | LSP4J版本过时 | 🟡 | ⬜ 待评估 |
| 17 | print()→logger | 🟡 | ⬜ 待修复（后端） |
| 18 | URL路径不一致 | 🟡 | ⬜ 待修复（后端） |
| 19 | 异常链丢失 | 🟡 | ⬜ 待修复（后端） |
| 20 | docstring缺失 | 🟢 | ⬜ 待修复（后端） |
| 21 | 参数过多 | 🟢 | ⬜ 待改进（后端） |
| 22 | 工具类分散 | 🟢 | ⬜ 待改进（大型重构） |
| 24 | 无JSON日志 | 🟢 | ⬜ 待改进（后端） |
| 25 | 无错误追踪 | 🟢 | ⬜ 待评估（后端） |
| 26 | model/model嵌套 | 🟢 | ⬜ 待改进（大型重构） |
| 27 | Docker权限过大 | 🟢 | ⬜ 待评估（后端） |
| 28 | SLF4J桥接缺失 | 🟢 | ⬜ 待改进 |
| M1 | 数据库迁移 mergepoint 过多 | 🟢 | ⬜ 待改进（后端） |
| L1 | 思考过程+工具调用不显示 | 🔴 | ✅ IDEA插件端已修复，后端协议已验证 |
| L2 | 代码Diff对比弹窗缺失 | 🟠 | ✅ 确认为 L1 级联问题，L1 修复后自动解决 |
| L3 | 智能体看不到本地项目 | 🔴 | 🔄 后端上下文注入已完整，需端到端验证 |
| L4 | 会话不流畅 | 🔴 | ✅ 与 L1 同根因，随全局 Map 修复解决 |
| L5 | think:: 围栏流式注入缺失 | 🟠 | ⬜ 待修复（后端为主） |
| 30 | LSP_EXECUTOR 永不关闭 | 🔴 | ⬜ 待修复（插件） |
| 31 | 心跳线程未命名/未守护（3处） | 🔴 | ⬜ 待修复（插件） |
| 32 | DatabaseChangeListener NPE风险 | 🟡 | ⬜ 待修复（插件） |
| 33 | invokeLater 过期引用 | 🟡 | ⬜ 待修复（插件） |
| 34 | @Contract 与可变状态矛盾 | 🟡 | ⬜ 待修复（插件） |
| 35 | DatabaseUtil 零日志 | 🟡 | ⬜ 待修复（插件） |
| 36 | ToolHandler 写操作无日志 | 🟡 | ⬜ 待修复（插件） |
| 37 | ToolInvokeProcessor 静默失败 | 🟡 | ⬜ 待修复（插件） |
| 38 | 6处 jsoup 内部API | 🟡 | ⬜ 待修复（插件） |
| 39 | 9文件缺中文Javadoc | 🟢 | ⬜ 待改进（插件） |
| 40 | WebSocket close 日志不当 | 🟢 | ⬜ 待修复（插件） |
| 41 | 长消息未截断 | 🟢 | ⬜ 待修复（插件） |
| 42 | 心跳异常日志未区分 | 🟢 | ⬜ 待修复（插件） |
| 43 | Jakarta WS 版本落后 | 🟢 | ⬜ 待评估 |
| 44 | xtend-lib-macro 冗余 | 🟢 | ⬜ 待清理 |
| 45 | flexmark + swingx 未维护 | 🟢 | ⬜ 跟踪上游替代方案 |
| 46 | L2 确认为 L1 级联 | 🟢 | ✅ 已确认 |

> 共 **43 个问题**（新增 17 个 #30-#46）：🔴 严重 8 个（#1, #3, #4, #6, #30, #31, L1, L3）、🟠 高 2 个（L2, L5）、🟡 中等 17 个（#8, #11, #13-#19, #32-#38）、🟢 低 16 个（#20-#22, #24-#28, #39-#45, M1）
>
> **本轮新增（2026-05-12 Round 3）**：
> - 17 个 IDEA 插件代码质量问题（#30-#46），来自 3 个并行 Agent 审查
> - #46 确认 L2 为 L1 级联问题，Diff 基础设施完整无需额外开发
> - **此前修复（Round 2）**：
>   - IDEA 插件端 L1/L4: `LanguageClientImpl.java` 新增 `ensureLsp4jRequestRegistered()`
>   - 后端 #12: `security.py` 添加 6 处安全关键操作日志
>   - 后端 #7: `restart.sh` cleanup 增加存活检测
>   - 完全移除: #7, #10, #12, #23, #29

# Clawith 后端 + IDEA 插件 全量代码评审 — 问题清单

> 评审时间：2026-05-12 | 更新：2026-05-14（第 12 轮，Terminal API 2025.1 迁移完成 + Qoder/Lingma 双品牌逻辑审查）
> 评审范围：Clawith 后端 `backend/` (365 个 Python 文件) + IDEA 插件 `demo-new/` (1361 个 Kotlin/Java 文件)
> Qodana 检查：5265 个问题（Critical 41 / High 3850 / Moderate 1374）

**共 53 个待处理问题**：🔴7　🟠4　🟡26　🟢14　💡2

> 本回合修复：6 项（#55/#56/#57 IDE 上下文持久化链路 + #62 前后端模型对齐 + #74 IDE WebSocket URL session_id + #76 IDE 日志关联）

---

## 🔴 严重（7 个）

### #1 — 智能体无法感知本地项目上下文

**原编号**: L3 | **位置**: `jsonrpc_router.py:942-1086` (`_build_lsp4j_ide_prompt`) + `chat_session.py` 模型
**状态**: ✅ 根因修复完成（#55/#56/#57），🔄 待端到端验证

**三层断裂**：
1. 上下文注入链路 — `_build_lsp4j_ide_prompt()` 已解析 `chatContext`（activeFilePath, selectedText 等）+ `sessionType` + `mode` + 代码地图注入 ✅；`_persist_lsp4j_chat_turn` 已持久化 `project_path`/`current_file`/`open_files` 到 `ChatSession` 模型 ✅
2. 工具可用性感知 — 系统提示词未说明"已连接本地IDE"，无首次连接环境注入消息
3. 工具调用反馈闭环 — UI 层看不到过程，LLM 层可能不知道如何利用返回结果

**已修复根因**（2026-05-14）：
- ✅ `ChatSession` 模型已添加 `client_type`, `project_path`, `current_file`, `open_files` 四个 `Mapped` 列声明
- ✅ `_persist_lsp4j_chat_turn` 创建/更新会话时持久化 IDE 上下文
- ✅ `chat/ask` 调用点提取 `chatContext` 传入持久化函数

<details>
<summary>LSP4J 未适配功能 & 修复规划</summary>

| 功能 | 协议方法 | 优先级 |
|------|---------|--------|
| 内联编辑操作 | `textDocument/nextEditAction` | P1 |
| 代码变更应用完成 | `chat/codeChange/apply/finish` | P1 |
| 快照同步 | `snapshot/syncAll` | P2 |
| 工作空间文件同步 | `workingSpaceFile/sync` | P2 |
| 聊天删除通知 | `chat/delete` | P2 |
| 全局配置变更 | `config/changeGlobal` | P2 |
| 模型列表刷新 | `config/refreshModels` | P2 |
| 代码引用查询 | `textDocument/queryReference` | P3 |
| PSI继承分析 | `psi/inherits` | P3 |
| PSI变量分析 | `psi/listVariables` | P3 |

| 阶段 | 内容 | 状态 |
|------|------|------|
| P0 | 全局 Map 初始化、tool/call/sync 通知、requestId 传递 | ✅ 已完成 |
| P1 | 增强上下文提取、增强工具可用性系统提示 | ⬜ 待推进 |
| 体验优化 | Web 端工具调用可视化、Diff 预览、高风险工具确认 | ⬜ 待规划 |

</details>

---

### #64 — `GET /api/chat/{agent_id}/history` 端点 `conversation_id` 与 WebSocket 会话系统不匹配

**来源**: 会话逻辑深度审查 (2026-05-14) | **位置**: `websocket.py:126-159` ↔ `Chat.tsx:442`
**状态**: ⬜ 待修复

前端 Chat.tsx 页面加载时调用 `GET /api/chat/{id}/history`，该端点硬编码 `conversation_id = f"web_{current_user.id}"`（旧格式）。但 WebSocket 保存消息时使用的 `conversation_id` 是 `ChatSession.id`（UUID 格式）。

**后果**: 通过 WebSocket 保存的消息无法通过 REST 历史端点查询，前端页面刷新后历史消息消失。

**修复**: 重构端点按当前主会话 ID 查询，增加 `session_id` 参数：
```python
@router.get("/api/chat/{agent_id}/history")
async def get_chat_history(
    agent_id: uuid.UUID,
    session_id: str = Query(None),
    ...
):
    conv_id = session_id or await get_primary_session_id(db, agent_id, current_user.id)
```

---

### #65 — WebSocket 连接无心跳 / 保活机制

**来源**: 会话逻辑深度审查 (2026-05-14) | **位置**: `websocket.py:162-982`
**状态**: ⬜ 待修复

WebSocket 连接建立后没有应用层 ping/pong 心跳。NAT 网关、反向代理 (nginx/haproxy) 在 60-120 秒无活动后断开空闲连接。

**修复**: 在消息循环中启动并发心跳任务（每 30s 发 `{"type": "ping"}`）。

参考: [RFC 6455 Section 5.5.2](https://datatracker.ietf.org/doc/html/rfc6455#section-5.5.2)

---

### #66 — ConnectionManager 无空闲连接超时清理

**来源**: 会话逻辑深度审查 (2026-05-14) | **位置**: `websocket.py:28-96` (`ConnectionManager`)
**状态**: ⬜ 待修复

`ConnectionManager` 只增不减——网络中断导致客户端没有发送 close frame 时，连接对象永远停留在内存中。

**后果**: 内存泄漏、达到 `MAX_CONNECTIONS=500` 后拒绝所有新连接。

**修复**: 添加连接时间戳 + 后台定期清理任务（空闲超过 300 秒移除）。

---

### #83 — 30s 硬超时误杀长工具会话

**来源**: 流畅度瓶颈分析 (2026-05-14) | **位置**: `BaseChatPanel.java:1335`
**状态**: ⬜ 待修复

固定 30s 超时对 `search_file` + `read_file` + `replace_text_by_path` 多轮工具调用链不够。IDE 端超时断开后后端继续执行但结果无法送达。

**修复**: 超时改为累计工具轮次感知（如累计 5 轮工具后放宽至 120s）。

---

### #84 — 上下文跨轮次无界增长

**来源**: 流畅度瓶颈分析 (2026-05-14) | **位置**: `caller.py:592` 循环
**状态**: ⬜ 待修复

每轮对话追加 user+assistant 到 `conversation[]`，无截断上限。长时间会话 LLM 推理延迟 +50-200%，token 费用指数增长。

**修复**: 增加轮次上限后上下文摘要/截断。

---

### #93 — `FINISH_PROTOCOL_REMINDER` 额外 LLM 轮

**来源**: 流畅度瓶颈分析 (2026-05-14) | **位置**: `caller.py:700`
**状态**: ⬜ 待修复

LLM 返回纯文本（无可调用工具）后，协议仍然发送 `FINISH_PROTOCOL_REMINDER` 触发额外推理轮，增加 5-15s 延迟。

**修复**: 文本无工具时自动视为 finish，跳过额外提醒轮。

---

## 🟠 高（4 个）

### #58 — WebSocket 会话鉴权 `!= "agent"` 意外放行 `trigger` 类型会话

**来源**: 会话逻辑专项审查 (2026-05-14) | **位置**: `websocket.py:282`
**状态**: ⬜ 待修复

```python
elif _existing.source_channel != "agent" and str(_existing.user_id) != str(user_id):
```

`source_channel` 可能值: `web`, `ide_lsp4j`, `feishu`, `discord`, `slack`, `agent`, `trigger`。条件 `!= "agent"` 覆盖了 `trigger`。

**修复**: 改为 `not in ("agent", "trigger")`

---

### #67 — `queued_messages` 被静默丢弃（用户并发消息丢失）

**来源**: 会话逻辑深度审查 (2026-05-14) | **位置**: `websocket.py:965-973`
**状态**: ⬜ 待修复

用户在 AI 回复期间发送的消息被静默丢弃，无任何通知。用户感知为"消息被吞"。

**修复**: 返回 `{"type": "info", "content": "当前回复尚未完成，请等待结束后重新发送"}`

---

### #85 — 每工具调用 4-5 条 WebSocket 通知

**来源**: 流畅度瓶颈分析 (2026-05-14) | **位置**: `jsonrpc_router.py:1877-1985`
**状态**: ⬜ 待优化

每个工具调用触发 PENDING → RUNNING → toolCall 块 → tool/call/sync → DONE 共 5 条通知，每条 WT 往返 +50ms。

**修复**: 合并 toolCall+sync 为单条消息。

---

### #86 — `chat/finish` 前强制 200ms sleep

**来源**: 流畅度瓶颈分析 (2026-05-14) | **位置**: `jsonrpc_router.py:2356`
**状态**: ⬜ 待修复

为保证 IDE 端渲染完最后一条消息再发送 finish 的 workaround。每轮 +200ms 延迟。

**修复**: 修复 IDE 竞态后移除 sleep。

---

## 🟡 中（25 个）

### #11 — 184 处 `except Exception:`（51 个文件），约 97 处静默吞异常

**原编号**: #8 | **位置**: `backend/app/api/agents.py:884-965`、`websocket.py`、`feishu.py` 等
**状态**: 🔄 部分修复（agents.py 8处已加日志，其余 89 处待评估）

```python
try: await agent_manager.remove_container(agent)
except Exception: pass
```

> `agent_tools.py` 的 57 处是工具加载兜底（合理保留），其余静默吞异常应至少加 `logger.warning(exc_info=True)`。

---

### #13 — OKR 模块绕过依赖注入

**原编号**: #13 | **位置**: `backend/app/api/okr.py` ~20 个端点
**状态**: 🔄 评估完成 — 需专项会话重构

OKR 路由直接创建 `async with async_session() as db:`，而项目其他模块统一 `Depends(get_db)`。

**建议**: 统一改为 `db: AsyncSession = Depends(get_db)`。

---

### #21 — LSP4J 主版本升级（0.12 → 1.0.0）需端到端验证

**原编号**: #51 | **位置**: `demo-new/gradle/libs.versions.toml`

lsp4j 主版本跳跃 + `javax → jakarta` 迁移。需验证 ping、chat/answer、chat/think、tool/invoke、tool/call/sync。

---

### #23 — 390 个 `catch(Exception)` 宽泛捕获（插件侧）

**新发现**: N9 | **位置**: 插件全局
**状态**: 🔄 会话管线全部修复（14 处已修复）

Chat/Core/LSP 模块优先改为具体异常类型。

---

### #24 — LanguageClientImpl 1322 行过长（30+ @Override 方法）

**新发现**: N10 | **位置**: `demo-new/.../core/lsp/LanguageClientImpl.java`
**状态**: ⬜ 待重构

`buildProject` → Delegate、`Completion` → 独立 Handler、工具结果同步 → 独立模块。

---

### #29 — Kotlin 仅占 ~5%（68/1361 文件），大量 POJO/Enum 可迁移

**新发现**: N15 | **位置**: 插件全局
**状态**: ⬜ 待规划

新文件强制 Kotlin、POJO/Enum 逐步转换。

---

### #31 — agent_tools.py 13609 行单体文件

**新发现**: N17 | **位置**: `backend/app/services/agent_tools.py`
**状态**: ⬜ 待评估

按功能域拆分（文件操作、搜索、Agent 通信、终端）可大幅提升可维护性。

---

### #51 — ConfigMainForm.java 372 个代码问题（Qodana 最高密度文件）

**来源**: Qodana | **位置**: `src/main/java/.../ui/config/ConfigMainForm.java`
**状态**: ⬜ 待重构

该文件 3218 行，建议拆分。

---

### #53 — 331 个魔术常量（Magic Constant）

**来源**: Qodana High | **位置**: 插件全局
**状态**: ⬜ 待评估

新代码强制使用常量，存量代码在修改相关逻辑时顺手提取。

---

### #59 — 会话删除时消息查询缺少 `agent_id` 过滤

**来源**: 会话逻辑专项审查 (2026-05-14) | **位置**: `chat_sessions.py:354`
**状态**: ⬜ 待修复

```python
await db.execute(sql_delete(ChatMessage).where(ChatMessage.conversation_id == str(session_id)))
```

**修复**: 添加 `.where(ChatMessage.agent_id == agent_id)` 条件

---

### #60 — 会话标题自动生成中 base64 图片数据残留

**来源**: 会话逻辑专项审查 (2026-05-14) | **位置**: `websocket.py:525`
**状态**: ⬜ 待修复

```python
clean_title = title_src.replace("[图片] ", "📷 ").replace("[image_data:", "").strip()
```

`replace("[image_data:", "")` 仅去掉前缀，base64 数据体仍在字符串中。

**修复**: 使用正则 `re.sub(r'\[image_data:[^\]]*\]', '', title_src)` 完整删除标记

---

### #61 — 未读计数 JOIN 使用 `cast(UUID→String)` 阻止索引利用

**来源**: 会话逻辑专项审查 (2026-05-14) | **位置**: `chat_sessions.py:111`
**状态**: ⬜ 待优化

`cast(UUID → String)` 使 PostgreSQL 无法使用 UUID 索引。

**建议**: 将 `ChatMessage.conversation_id` 改为 `UUID` 类型，或将 `session_uuid_ids` 转为字符串列表直接 `IN` 匹配。

---

### #68 — 单条 WebSocket 消息触发 5-9 次独立 DB 事务

**来源**: 会话逻辑深度审查 (2026-05-14) | **位置**: `websocket.py` 消息处理主循环
**状态**: ⬜ 待优化

每条用户消息处理创建多达 9 次 `async_session()`：(a) 吞吐量下降；(b) 部分失败时前后不一致。

**修复**: 配额检查+用户消息保存可共用同一 `async_session()`。

---

### #69 — LLM 错误检测依赖字符串前缀匹配（脆弱）

**来源**: 会话逻辑深度审查 (2026-05-14) | **位置**: `websocket.py:874-878`
**状态**: ⬜ 待优化

```python
_LLM_ERROR_PREFIXES = ("[LLM Error]", "[LLM call error]", "[Error]")
```

如果 LLM 恰好返回以 `[Error]` 开头的合法文本，会被误判为调用失败。

**修复**: 在 `call_llm_with_failover` 中使用异常传播，或增加 `is_error` 标志位。

---

### #70 — 会话列表无分页，无法应对规模化

**来源**: 会话逻辑深度审查 (2026-05-14) | **位置**: `chat_sessions.py:63-195`
**状态**: ⬜ 待优化

`result.scalars().all()` 无 `limit/skip`，可能返回数千条。

**修复**: 添加 `skip`/`limit` 查询参数。

---

### #71 — ChatMessage 缺少 `(agent_id, conversation_id, created_at)` 复合索引

**来源**: 会话逻辑深度审查 (2026-05-14) | **位置**: `models/audit.py`
**状态**: ⬜ 待优化

当前仅有独立索引，最核心的查询模式（`WHERE agent_id=? AND conversation_id=? ORDER BY created_at`）未覆盖。

**修复**: 添加 `Index("ix_chat_messages_agent_conv_created", "agent_id", "conversation_id", "created_at")`

---

### #72 — 会话上下文截断可能拆分 tool_call 对

**来源**: 会话逻辑深度审查 (2026-05-14) | **位置**: `websocket.py:782-784`
**状态**: ⬜ 待优化

当前代码只保证不移除以 `tool` 开头的片段，但未保证截断后不产生孤立的 `tool` 消息。

**修复**: 从后往前扫描，确保每个 `tool` 消息有对应的 `assistant + tool_calls` 前导。

---

### #73 — `websocket.py` 982 行单体文件，职责过载

**来源**: 会话逻辑深度审查 (2026-05-14) | **位置**: `backend/app/api/websocket.py`
**状态**: ⬜ 待重构

- `ConnectionManager` → `app/services/connection_manager.py`
- 会话加载逻辑 → `app/services/chat_session_service.py`
- LLM 调用封装 → `app/services/llm_stream_handler.py`
- WebSocket 路由保持瘦层（~200 行）

---

### #81 — JWT 过期无用户通知

**来源**: 插件 UI 优化 (2026-05-14) | **位置**: `CosyWebSocketConnectClient.java` `onClose()`
**状态**: ⬜ 待修复

WebSocket 返回 4001/4002 认证失败码时，插件只记录日志不通知用户。

**修复**: 4001/4002 时弹出 IntelliJ Notification 告知重新登录。

---

### #87 — `future.get()` 同步阻塞后台线程

**来源**: 流畅度瓶颈分析 (2026-05-14) | **位置**: `LanguageWebSocketService.java:329`
**状态**: ⬜ 待优化

`future.get()` 同步阻塞线程等待工具结果返回，整轮无法并发处理。

**修复**: 改用 CompletableFuture 异步回调。

---

### #88 — 工具上下文 Java + Python 双重注入

**来源**: 流畅度瓶颈分析 (2026-05-14) | **位置**: `BaseChatPanel.java:2170` + `jsonrpc_router.py:1633`
**状态**: ⬜ 待优化

Java 侧和 Python 侧各自注入工具调用上下文到 LLM 提示，浪费 token。

**修复**: 去掉 Java 侧注入，仅保留 Python 侧。

---

### #89 — 写工具串行阻塞整轮

**来源**: 流畅度瓶颈分析 (2026-05-14) | **位置**: `caller.py:793`
**状态**: ⬜ 待优化

同文件无依赖的写工具（如 `edit_file` + `write_file` 不同文件）仍串行执行。

**修复**: 同文件无依赖写工具可并行。

---

### #90 — `toolCallId` 队列失配产生僵尸卡片

**来源**: 流畅度瓶颈分析 (2026-05-14) | **位置**: `jsonrpc_router.py:3042-3063`
**状态**: ⬜ 待修复

工具调用队列顺序与 IDE 侧 toolCallId 分配不一致时创建无法匹配的僵尸卡片，UI 卡死。

**修复**: 添加队列顺序校验 + 回退匹配。

---

### #91 — 冷缓存 DB 历史加载阻塞 LLM

**来源**: 流畅度瓶颈分析 (2026-05-14) | **位置**: `jsonrpc_router.py:1601-1624`
**状态**: ⬜ 待优化

首次请求时 DB 历史加载串行阻塞 LLM 启动，增加 50-500ms 延迟。

**修复**: 延长缓存 TTL + 异步预加载。

---

### #92 — 每轮强制快照同步

**来源**: 流畅度瓶颈分析 (2026-05-14) | **位置**: `jsonrpc_router.py:1544-1549`
**状态**: ⬜ 待优化

每轮 `chat/ask` 都执行 `get_or_create_snapshot` + `syncAll`，增加 100-500ms 延迟。

**修复**: 仅文件变更时同步。

---

### #95 — `com.intellij.terminal` 包下 6 个类在 IntelliJ Platform 2025.1 中被移除

**来源**: 编译验证 (2026-05-14) | **位置**: `RunTerminalToolHandlerV2.java`, `TerminalUtils.java`, `TerminalCommandExecutor.java`, `TerminalService.kt`
**状态**: ✅ 已修复

IntelliJ Platform 2025.1 重构了 Terminal 插件 API，`com.intellij.terminal` 包下的 `ShellTerminalWidget`、`TerminalView`、`TerminalUtil`、`TerminalWorkingDirectoryManager`、`TerminalOptionsProvider`、`TerminalProjectOptionsProvider` 被移除或迁移。

**修复方案**：

| 旧 API | 替代 |
|--------|------|
| `com.intellij.terminal.ShellTerminalWidget` | `org.jetbrains.plugins.terminal.ShellTerminalWidget` |
| `com.intellij.terminal.TerminalView` | `org.jetbrains.plugins.terminal.TerminalToolWindowManager` |
| `TerminalUtil.hasRunningCommands()` | `JBTerminalWidget.hasRunningCommands()` |
| `TerminalWorkingDirectoryManager.getWorkingDirectory()` | widget 自带方法 |
| `TerminalOptionsProvider` | 删除（Registry 反射已覆盖） |
| `TerminalProjectOptionsProvider` | `org.jetbrains.plugins.terminal.TerminalProjectOptionsProvider` |

**修改文件**: `TerminalUtils.java` (import 改包), `TerminalCommandExecutor.java` (import 改包 + 方法调用适配), `RunTerminalToolHandlerV2.java` (TerminalView → TerminalToolWindowManager + hasRunningCommands 适配), `TerminalService.kt` (ShellTerminalWidget 包名适配)

---

### #96 — 插件中 Qoder/Lingma 双品牌逻辑待统一为 Clawith

**来源**: 代码审查 (2026-05-14) | **位置**: 8 个文件，16 处 `isQoderEnabled()` 调用
**状态**: ⬜ 待修复

插件当前通过 `CosyConfig.isQoderEnabled()` 维护两套品牌逻辑，在 Qoder（精简版）和 Lingma（完整版）之间切换。影响面：

| 文件 | 行数 | 影响 |
|------|------|------|
| `CosyConfig.java` | 3 处 | 存储目录 `.qoder`/`.lingma` + 方法定义 + `useNewNesFeature()` |
| `CosySetting.java` | 2 处 | `methodQuickSwitchEnum` 默认值 + `getDefaultDisplayLanguage()` |
| `ProcessUtils.java` | 1 处 | `getCosyPath()` 目录 |
| `LingmaBaseCreationAction.kt` | 1 处 | 产品目录名 |
| `LingmaUrls.java` | 1 处 | Qoder 专用 URL vs Lingma 环境 URL |
| `RunTerminalToolHandlerV2.java` | 1 处 | 终端名 `QoderTerminal` / `LingmaTerminal` |
| `InlineChatPanel.java` | 1 处 | `!isQoderEnabled()` 条件控制组件显隐 |
| `ConfigMainForm.java` | 6 处 | UI 面板显隐控制 |

**修复方向**：
- 删除 `isQoderEnabled()` 方法
- 所有双路判断统一走非 Qoder 路径（完整功能路径）
- 品牌名统一为 Clawith：存储目录 → `.clawith`，终端名 → `ClawithTerminal`，产品目录 → `.clawith`
- 默认语言保持中文，methodQuick 默认启用

**参考**: 问题清单中，与 Qoder 相关的 UI 文案（`messageBundle*.properties`）也待同步清理。

---

## 🟢 低（14 个）

### 后端（6 个）

| # | 问题 | 原编号 | 位置 | 建议 |
|----|------|--------|------|------|
| 34 | `list_tasks()` 12 个参数 | #21 | `api/tasks.py` | 封装为 Pydantic Model，暂缓 |
| 36 | 无错误追踪集成 | #25 | 后端整体 | 可选 Sentry SDK |
| 37 | Docker SYS_ADMIN + docker.sock | #27 | `docker-compose.yml` | 非沙箱场景移除权限 |
| 63 | 多处文件内重复导入 | 新增 | `websocket.py`, `chat_sessions.py`, `jsonrpc_router.py` | 统一在文件顶部导入 |
| 77 | ChatSession 模型无软删除和状态字段 | 会话深度审查 | `models/chat_session.py` | 添加 `deleted_at`, `status`, `message_count` |
| 78 | 日志级别不一致——高频 INFO 淹没关键信息 | 会话深度审查 | `websocket.py:387,530,960` | 改为 DEBUG 级 |

### IDEA 插件（5 个）

| # | 问题 | 原编号 | 位置 | 建议 |
|----|------|--------|------|------|
| 44 | 76 个工具类分散 | #22 | `util/` 包 | 按功能合并 |
| 45 | `model/model` 路径嵌套 | #26 | `core/lsp/model/model/` | 重构为 `core/lsp/model/` |
| 47 | flexmark/swingx 未维护 | #45 | `libs.versions.toml` | 跟踪替代方案 |
| 49 | 9 个 ChatXxxProcessor 重复模式 | #69 | `chat/processor/` | 提取基类 |
| 54 | Qodana 样式问题（大批量） | Qodana | 插件全局 | 日常迭代逐步修复 |

### 插件 UI 优化（3 个）

| # | 问题 | 位置 | 方案 |
|---|------|------|------|
| 79 | 设置页登录后不显示当前智能体名 | `ClawithConfigurable.java` | 增加 `agentNameLabel` |
| 80 | 聊天下拉智能体无角色描述 | `ChatModeItem.java` | tooltip 显示 `name + "\n" + roleDesc` |
| 82 | URL 输入框无 placeholder | `ClawithConfigurable.java` | 添加 placeholder 提示 |

---

## 💡 低风险 / 流畅度备注（2 个）

| # | 瓶颈 | 位置 | 延迟量 | 修复方向 |
|---|------|------|--------|----------|
| 75 | ChatSessionUtil 全局 Map 存储 request_id→sessionType | `ChatSessionUtil.java:252-269` | — | 已使用 UserData，低风险保留 |
| 94 | `_UI_HEAVY_SEARCH_TOOLS` 卡片闪烁 | `jsonrpc_router.py:1896-1909` | UI 抖动 | 快速工具延迟卡片创建 |

---

## 汇总

| # | 问题 | 严重程度 | 来源 | 状态 |
|---|------|----------|------|------|
| 1 | 智能体看不到本地项目 | 🔴 | 人工评审 | 🔄 根因已修复，待验证 |
| 64 | conv_id 与 WebSocket 不匹配 | 🔴 | 会话深度审查 | ⬜ |
| 65 | WebSocket 无心跳/保活 | 🔴 | 会话深度审查 | ⬜ |
| 66 | ConnectionManager 无超时清理 | 🔴 | 会话深度审查 | ⬜ |
| 83 | 30s 硬超时误杀长工具 | 🔴 | 流畅度分析 | ⬜ |
| 84 | 上下文跨轮次无界增长 | 🔴 | 流畅度分析 | ⬜ |
| 93 | FINISH_PROTOCOL_REMINDER 额外轮 | 🔴 | 流畅度分析 | ⬜ |
| 58 | WebSocket 鉴权放行 trigger | 🟠 | 会话专项审查 | ⬜ |
| 67 | queued_messages 静默丢弃 | 🟠 | 会话深度审查 | ⬜ |
| 85 | 每工具 4-5 条 WS 通知 | 🟠 | 流畅度分析 | ⬜ |
| 86 | chat/finish 前 200ms sleep | 🟠 | 流畅度分析 | ⬜ |
| 11 | 97处静默异常 | 🟡 | 人工评审 | 🔄 部分修复 |
| 13 | OKR绕过DI | 🟡 | 人工评审 | 🔄 需专项会话 |
| 21 | LSP4J 1.0 端到端验证 | 🟡 | 人工评审 | ⬜ |
| 23 | 390 个 catch(Exception)（插件） | 🟡 | 人工评审 | 🔄 会话管线全修复 |
| 24 | LanguageClientImpl 1322 行 | 🟡 | 人工评审 | ⬜ |
| 29 | Kotlin 仅占 5% | 🟡 | 人工评审 | ⬜ |
| 31 | agent_tools.py 13609 行 | 🟡 | 人工评审 | ⬜ |
| 51 | ConfigMainForm.java 372问题 | 🟡 | Qodana | ⬜ |
| 53 | 331 个魔术常量 | 🟡 | Qodana | ⬜ |
| 59 | 会话删除缺 agent_id 过滤 | 🟡 | 会话专项审查 | ⬜ |
| 60 | 标题生成 base64 残留 | 🟡 | 会话专项审查 | ⬜ |
| 61 | 未读查询 cast UUID→String | 🟡 | 会话专项审查 | ⬜ |
| 68 | 单条消息 5-9 次 DB 事务 | 🟡 | 会话深度审查 | ⬜ |
| 69 | LLM 错误前缀匹配脆弱 | 🟡 | 会话深度审查 | ⬜ |
| 70 | 会话列表无分页 | 🟡 | 会话深度审查 | ⬜ |
| 71 | ChatMessage 缺复合索引 | 🟡 | 会话深度审查 | ⬜ |
| 72 | 上下文截断拆 tool_call 对 | 🟡 | 会话深度审查 | ⬜ |
| 73 | websocket.py 982行单体 | 🟡 | 会话深度审查 | ⬜ |
| 81 | JWT 过期无用户通知 | 🟡 | 插件UI优化 | ⬜ |
| 87 | future.get() 同步阻塞 | 🟡 | 流畅度分析 | ⬜ |
| 88 | 工具上下文双重注入 | 🟡 | 流畅度分析 | ⬜ |
| 89 | 写工具串行阻塞 | 🟡 | 流畅度分析 | ⬜ |
| 90 | toolCallId 队列失配 | 🟡 | 流畅度分析 | ⬜ |
| 91 | 冷缓存 DB 阻塞 LLM | 🟡 | 流畅度分析 | ⬜ |
| 92 | 每轮强制快照同步 | 🟡 | 流畅度分析 | ⬜ |
| 96 | Qoder/Lingma 双品牌逻辑 | 🟡 | 代码审查 | ⬜ |
| 34 | 参数过多 | 🟢 | 人工评审 | 🔄 暂缓 |
| 36 | 无错误追踪 | 🟢 | 人工评审 | ⬜ |
| 37 | Docker权限过大 | 🟢 | 人工评审 | ⬜ |
| 63 | 多处重复导入 | 🟢 | 会话专项审查 | ⬜ |
| 77 | ChatSession 无软删除 | 🟢 | 会话深度审查 | ⬜ |
| 78 | 日志级别不一致 | 🟢 | 会话深度审查 | ⬜ |
| 44 | 工具类分散 | 🟢 | 人工评审 | ⬜ |
| 45 | model/model嵌套 | 🟢 | 人工评审 | ⬜ |
| 47 | flexmark/swingx 未维护 | 🟢 | 人工评审 | ⬜ |
| 49 | 9 个 Processor 重复 | 🟢 | 人工评审 | ⬜ |
| 54 | Qodana 样式问题 | 🟢 | Qodana | ⬜ |
| 79 | 设置页缺智能体名 | 🟢 | 插件UI优化 | ⬜ |
| 80 | 下拉缺角色描述 | 🟢 | 插件UI优化 | ⬜ |
| 82 | URL 框缺 placeholder | 🟢 | 插件UI优化 | ⬜ |
| 75 | ChatSessionUtil 全局Map | 💡 | 流畅度分析 | ⬜ 低风险 |
| 94 | UI 搜索卡片闪烁 | 💡 | 流畅度分析 | ⬜ |

> **本轮修复（6 项）**：#55 ChatSession 模型字段 ✅ + #56 SessionContextManager 集成 ✅ + #57 IDE 上下文持久化 ✅ + #62 前后端模型对齐 ✅ + #74 IDE WebSocket URL session_id ✅ + #76 IDE 日志 session_id 关联 ✅
>
> **历史已修复（63 项）**：#95 Terminal API 2025.1 迁移 ✅ + 原 #2, #3, #4, #7, #10, #12, #14, #15, #16, #17, #18, #22, #26, #27, #28, #32, #33, #35, #38, #39, #40, #41, #42, #43, #46, #50, #51, #52, #53, #56, #57-#68 + L1-L5 等